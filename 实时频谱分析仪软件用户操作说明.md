# RFSoC ZU47DR 实时频谱分析仪软件用户操作说明

## 1. 适用范围

本文说明当前一期 PC 模拟版的安装、启动、频谱操作、模拟场景配置、文件导出、日志和故障排查。当前版本使用软件生成频域数据；PL、AXI DMA 和目标板幅度标定尚未接入，因此界面中的模拟幅度默认使用 dBFS 并标记为“未校准”。

## 2. 启动方式

Windows 可直接双击的完整部署版可执行文件位于：

```powershell
build\install-final\bin\rtsa_app.exe
```

必须保留整个 `build\install-final` 目录，不能只复制单独的 EXE；Qt、MinGW
动态库和 `platforms\qwindows.dll` 已随完整安装目录部署。

常用启动参数：

```powershell
# 默认模拟数据源
build\install-final\bin\rtsa_app.exe --source simulated

# 加载可重复的默认 JSON 场景
build\install-final\bin\rtsa_app.exe --source simulated `
  --scenario build\install-final\share\rtsa\config\default-simulation.json

# 全屏启动
build\install-final\bin\rtsa_app.exe --source simulated --fullscreen

# 自动采集 3 秒后正常退出，适合冒烟测试
build\install-final\bin\rtsa_app.exe --source simulated --smoke-test-ms 3000

# 查看全部参数
build\install-final\bin\rtsa_app.exe --help
```

数据源参数只接受 `simulated` 和 `dma`。`--scenario` 只能与 `--source simulated` 一起使用。在正式 DMA 协议和 Linux 用户态 API 尚未提供前，选择 `--source dma` 会明确返回错误并退出，不会静默显示模拟数据。

## 3. 日志参数

正常启动时软件写入有界滚动日志。默认日志目录为 Qt 的应用本地数据目录下的 `logs` 子目录，当前文件名为 `rtsa.log`，历史文件依次为 `rtsa.log.1`、`rtsa.log.2` 等。

```powershell
build\install-final\bin\rtsa_app.exe `
  --log-dir D:\RTSA-Logs `
  --log-level info `
  --log-max-bytes 5242880 `
  --log-retained-files 3
```

- `--log-level`：`debug`、`info`、`warning` 或 `error`；
- `--log-max-bytes`：单个日志文件上限，至少 1,024 字节；
- `--log-retained-files`：保留 1～10 个轮转文件；
- `--no-file-log`：仅建议自动测试或临时诊断使用。

每条日志包含 UTC 时间、级别、模块、事件和原因，换行及字段分隔符会被转义，避免破坏记录结构。

## 4. 主界面布局

主界面由频谱绘图区、可滚动参数面板、操作按钮和状态诊断区组成。

- 频谱绘图区：坐标、主/次网格、当前轨迹和 Marker；
- 数据源/频率参数：Center、Span、Start、Stop、FFT 点数和输入帧率；
- 幅度参数：参考电平、底部电平、dB/格、自动量程、曲线/网格和绘图区主题；
- 模拟参数：噪声、两个单音、扫频和瞬态；
- 轨迹与 Marker：Clear/Write、Average、Max/Min Hold、M1～M4、Delta 和相邻峰；
- 区间测量：指定范围峰值和信道功率；
- 状态区：源状态、输入/显示 FPS、平均数据率、丢帧、处理/绘制耗时、帧龄和显示延迟 P95。

非模拟数据源不会显示模拟参数组，频谱绘图控件本身不随数据源更换而重建。

状态区的“源/传输丢帧”只统计数据源自身未能形成或交付的帧；“无效帧”和“处理丢帧”由处理流水线分类；“显示跳过”按流水线发布序号计算，不会把 PL/模拟源的输入序号跳变误算为 GUI 丢帧。显示延迟 P95 使用最近 256 个真正完成 `paintEvent()` 的不同发布帧样本，而不是仅提交给绘图控件的时间。

## 5. 基本频谱操作

### 5.1 频率范围

- `Center/Span` 与 `Start/Stop` 双向同步；
- 修改任意一组并确认后，另一组自动更新；
- 鼠标滚轮以指针位置为锚点缩放 Span；
- 按住鼠标左键拖动可平移频率范围；
- 按住 Shift 后左键拖动可框选频率范围；
- 触控屏支持 Pinch 缩放和 Pan 平移；
- 双击绘图区或按 `Home` 恢复用户设置的完整频率范围。

### 5.2 幅度范围

- 参考电平控制纵轴顶部；
- 底部电平必须低于参考电平；
- dB/格用于调整垂直刻度；
- 自动量程根据当前有效轨迹设置可视范围。
- 曲线颜色、1～4 px 线宽、网格和深浅绘图区主题可实时切换。

### 5.3 采集控制

- `开始`：启动连续采集；
- `暂停/继续`：保留最后有效频谱与测量；
- `单次`：数据源只发布一帧后自动停止；
- `停止`：唤醒采集线程并等待其正常退出，不强制终止线程。

退出窗口前软件会停止数据源并等待后台 CSV 导出完成。

## 6. 模拟信号配置

### 6.1 噪声和固定信号

- 噪声底单位为 dBFS；
- 噪声起伏控制随机变化；
- 两个固定单音分别配置启用状态、相对中心频率偏移和幅度；
- 每个固定单音可独立设置等效带宽，范围 0.1～1,024 bins；
- 固定随机种子保证同一配置可重复生成。

### 6.2 扫频信号

- 可启用或关闭扫频；
- 方向支持向上、向下和往返；
- 起点和终点以相对 Center 的 MHz 偏移独立配置，且起点必须小于终点；
- 周期范围为 0.1～3,600 秒；
- 幅度单位为 dBFS；
- 默认扫频范围位于 Center ±70 MHz，用户可通过起止偏移修改；非法的倒置区间会自动修正。

### 6.3 瞬态信号

- 概率以“每帧百分比”设置；
- 幅度单位为 dBFS；
- 持续时间范围为 0.001～60 秒；
- 一次瞬态触发后在持续时间内保持同一频率，而不是每帧随机跳变。

### 6.4 FFT 点数和帧率

支持 1,024、2,048、4,096、8,192、16,384、32,768 和 65,536 点。受限输入帧率范围为 1～1,000 FPS；启用“无等待压测”后采集线程尽可能快地生成完整帧，输入帧率控件暂时禁用。该模式会主动占用一个 CPU 核心，仅用于管线性能测试。无论输入多快，GUI 都只显示刷新时刻的最新完整帧，不建立无界队列。

### 6.5 JSON 模拟场景

项目交付三个可直接加载的场景：

- `config/default-simulation.json`：默认功能演示；
- `config/unthrottled-stress.json`：无等待吞吐压测。
- `config/fault-injection.json`：周期数据暂停、序号跳变、NaN 异常帧和突发输出。

场景包含频点数、帧率、Center/Span、噪声、随机种子、最多两个固定信号、扫频、瞬态和故障参数。格式版本当前为 2，并兼容版本 1（故障全部关闭）；软件严格校验文件大小、JSON 类型、数值范围、数组上限、半开频率上界、扫频区间和故障参数，失败时退出而不使用部分配置。场景参数优先于已保存的用户设置，并会同步显示到界面控件。

在界面中修改参数后，点击文件区的“保存模拟场景”即可导出 JSON。保存采用原子文件提交，并在写入前用与加载相同的规则验证完整场景；失败不会留下看似有效的目标文件。文件对话框负责已有文件的覆盖确认。

## 7. 轨迹、Marker 与测量

- Clear/Write：实时覆盖；
- Average：按设置的平均次数平滑；
- Max Hold：保留各频点历史最大值；
- Min Hold：保留各频点历史最小值；
- 配置的 FFT 点数、频率范围或幅度单位变化后，不兼容历史轨迹自动清空；
- 活动标记可选 M1～M4，单击绘图区设置当前活动 Marker；
- Peak Search 使用完整频点寻找最大值，Next/Previous 按门限搜索相邻局部峰；
- Delta 以 M1 为参考，显示活动 M2～M4 的频差和幅差；
- Marker 以频率保存身份，每帧重新映射并更新幅度，移出当前 Span 后显示无效；
- 区间峰值和信道功率直接使用完整频点；dB 数据先转线性功率积分再转回 dB；
- 所有读数显示单位、有效状态以及已校准/未校准状态。

dBFS 表示相对于数字满量程的值。没有正式硬件标定时不得把它解释为准确 dBm。

## 8. 截图、CSV 与配置

### 8.1 PNG 截图

点击截图按钮后选择目标文件。截图来自 CPU Raster 频谱控件，不依赖 OpenGL。

### 8.2 CSV 导出

CSV 在后台写入，导出对象为点击时的不可变完整频谱帧，包含：

- 帧序号、时间戳、Center、Span 和点数；
- 幅度单位及是否校准；
- 每个频点的索引、频率和幅度。

导出使用原子文件提交；失败时不会留下看似完整的半写目标文件。

### 8.3 自动配置

软件保存窗口、频率、点数、帧率/无等待模式、噪声、单音带宽、扫频范围/方向、瞬态、幅度、轨迹和 Raster 外观参数。配置格式当前为版本 4；版本 1～3 和旧无版本配置可迁移，未知未来版本或损坏值会整体回退到安全默认值。

## 9. 全屏与目标 Linux

- 点击全屏按钮进入或退出全屏；
- `Esc` 退出全屏；
- PetaLinux 无 GPU 场景建议：

```sh
export QT_QPA_PLATFORM=linuxfb
export QT_QPA_FB_HIDECURSOR=1
./rtsa_app --source simulated --fullscreen
```

实际 QPA、字体、PNG 插件和输入设备必须以目标 rootfs 为准。

## 10. 常见故障

| 现象 | 原因与处理 |
|---|---|
| Windows 提示缺少 DLL | 使用完整 `build\install-final` 目录，不要单独复制 EXE；确认 `bin\platforms\qwindows.dll` 存在 |
| 右侧输入框文字与背景接近 | 使用最新安装版；新版固定使用 Fusion 高对比度主题并分别设置正常、非活动和禁用颜色 |
| `WrapVulkanHeaders` 缺失 | Qt 可选探测提示；本项目不使用 Vulkan/OpenGL，不影响构建 |
| `--source dma` 退出 | 正常保护行为；需先提供 PL 协议和 DMA 用户态 API |
| 显示 dBFS“未校准” | 硬件标定尚未完成，不能当作准确 dBm |
| linuxfb 无文字 | rootfs 缺少字体或字体配置，需加入可用字体 |
| CSV 按钮暂不可用 | 尚无有效完整帧，先开始或执行单次采集 |

## 11. 当前未完成的外部验收

- Qt 5.15 实际编译；
- PetaLinux 2023.2 SDK ARM64 交叉编译；
- ZU47DR 板端 linuxfb/xcb 显示；
- 24 小时板端长稳和内存趋势；
- 真实 DMA 吞吐、丢帧、延迟、帧协议和幅度标定。

这些项目不能用 Windows 模拟测试结果替代。
