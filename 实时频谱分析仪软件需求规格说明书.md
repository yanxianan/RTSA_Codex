# 基于 RFSoC ZU47DR 的实时频谱分析仪 PS 端软件需求规格说明书

| 项目 | 内容 |
|---|---|
| 文档版本 | V0.1 |
| 文档状态 | 初始需求基线 |
| 编制日期 | 2026-07-25 |
| 目标平台 | RFSoC ZU47DR PS 端（项目按无可用GPU加速设计） |
| 目标系统 | PetaLinux 2023.2，AArch64 Linux |
| PC 开发环境 | Windows，Qt 6.x |
| 目标端 Qt 基线 | Qt 5.15.4（PetaLinux 2023.2 标准 `meta-qt5` 发布分支） |
| 一期数据源 | 软件模拟频谱数据 |
| 后续数据源 | PL 经 AXI DMA 向 PS DDR 传输的 FFT 频谱结果 |

## 1. 文档目的

本文档定义运行于 RFSoC ZU47DR PS 端 Linux 系统中的实时频谱分析仪软件需求，作为软件设计、编码、测试、交叉编译、部署及后续 PL/PS 联调的依据。

当前 PL 侧 DDC、FFT 和 AXI DMA 数据通路尚未完成，因此一期首先在 Windows PC 上使用模拟数据源完成上位机的界面、频谱绘制、交互、测量、数据管理和性能框架。后续接入真实硬件时，通过替换数据源适配器和协议解析模块完成 AXI DMA 接入，频谱显示及业务模块不应因数据源变化而重写。

## 2. 项目背景与目标

### 2.1 项目背景

系统硬件平台为 RFSoC ZU47DR。PL 侧负责射频数据处理，包括但不限于 DDC、加窗、FFT 及频谱数据生成；PS 侧不承担大带宽原始 ADC 数据的 FFT 运算，主要负责：

- 接收 PL 产生的频谱结果；
- 将硬件数据转换为软件内部统一频谱帧；
- 实时显示频谱曲线；
- 提供常用频谱分析仪交互和测量功能；
- 显示运行状态、数据率、帧率和异常信息；
- 为瀑布图、多通道和硬件控制等后续功能预留扩展能力。

### 2.2 一期目标

1. 在 Windows PC 上完成可独立运行的模拟版软件。
2. 模拟数据源能够产生具有噪声、单音、多音、扫频和瞬态特征的频谱数据。
3. 在无 GPU 加速假设下，实现高性能、低延迟、可长期运行的实时频谱显示。
4. 软件源代码同时兼容 Qt 6.x 和 PetaLinux 2023.2 所采用的 Qt 5.15.x。
5. 建立与数据来源无关的频谱处理和显示架构。
6. 给出后续 AXI DMA 数据接入接口、协议约束和联调验收要求。
7. 完成 ARM64 目标版本的交叉编译、部署和板端运行验证。

### 2.3 长期目标

- 接入真实 AXI DMA 连续数据流；
- 增加瀑布图；
- 支持多通道频谱显示；
- 增加更多频域测量和告警功能；
- 根据硬件协议控制 DDC、FFT、窗函数、平均次数等 PL 参数；
- 支持频谱数据记录、回放及远程控制。

## 3. 需求范围

### 3.1 一期范围内

- Qt/C++ 桌面式上位机；
- Windows 模拟运行版本；
- PetaLinux ARM64 交叉编译版本；
- 模拟频谱数据发生器；
- 实时频谱曲线及坐标系统；
- 频谱常用交互、轨迹和标记功能；
- 状态监控、配置保存、截图及频谱数据导出；
- 高性能 CPU 绘制和线程解耦；
- Qt 5/Qt 6 兼容层；
- 抽象数据源接口和 DMA 接入接口定义；
- 单元测试、集成测试和性能测试要求；
- 交叉编译与部署说明。

### 3.2 一期范围外

- PL 侧 DDC、加窗、FFT 和 AXI Stream 逻辑实现；
- Linux 内核 AXI DMA 驱动开发；
- 射频前端及 ADC 配置；
- 原始 ADC/IQ 数据的 PS 端 FFT 处理；
- 瀑布图正式实现；
- 多通道同时显示；
- ACP、EVM、调制分析、解调等高级信号分析；
- 远程网络控制和 SCPI 接口；
- 长时间原始高速数据无损存盘。

以上范围外项目可以定义接口或提出建议，但不作为一期交付验收的阻塞条件。

## 4. 已确认条件、设计假设与待确认项

### 4.1 已确认条件

- 软件直接运行于 RFSoC ZU47DR 的 PS 端 Linux 系统中，而非外部 PC 上位机。
- 目标 Linux 由 PetaLinux 2023.2 构建。
- PC 开发环境为 Windows，当前可使用 Qt 6.x。
- PL 负责 DDC 和 FFT，PS 接收频谱结果后显示。
- PL 与 PS 计划通过 AXI DMA 传输数据。
- PL 逻辑暂未完成，一期使用软件模拟数据。
- 一期只正式实现频谱显示，瀑布图等作为后续扩展。
- 屏幕暂定为 1920×1080、60 Hz。
- 项目按目标板无可用GPU或无可用GPU驱动设计，一期不依赖OpenGL硬件加速。

### 4.2 设计假设

- PS 端为 AArch64 架构，具备可运行 Qt Widgets 应用的显示输出通路。
- 目标系统至少能够提供 Linux framebuffer、X11/XCB 或其他支持 Qt Raster 绘制的 QPA 平台之一。
- PL 最终输出的是已经形成频点序列的幅度或功率数据，而非连续时域 ADC 数据。
- DMA 数据写入 PS DDR，用户态通过现有或后续提供的驱动接口取得完整数据帧。
- UI 刷新率无需等于输入频谱帧率；当输入过快时允许丢弃过期的显示帧，但采集和丢帧状态必须可统计。
- 软件内部以 `float32` 表示频点显示值，默认单位为 dBFS；真实标定关系确定后允许切换为 dBm 或其他单位。

### 4.3 待 PL/硬件完成后确认

| 编号 | 待确认内容 | 对软件的影响 |
|---|---|---|
| TBD-01 | FFT 点数及可选范围 | 缓冲区大小、横轴映射、性能上限 |
| TBD-02 | 每个频点的位宽、符号、定点格式及字节序 | DMA 协议解析器 |
| TBD-03 | PL 输出幅度、功率、幅度平方、对数值或复数值 | 标度转换公式 |
| TBD-04 | FFT Shift 和频点排列顺序 | 横轴频率映射 |
| TBD-05 | AXI Stream 位宽、`TLAST` 语义和帧间空隙 | DMA 帧边界识别 |
| TBD-06 | AXI DMA 的 Simple/Scatter-Gather 模式 | 驱动接口和缓冲策略 |
| TBD-07 | 实际帧率、峰值数据率和允许丢帧策略 | 性能验收参数 |
| TBD-08 | 帧序号、时间戳及中心频率等元数据来源 | 数据帧结构 |
| TBD-09 | dBFS/dBm 标定公式、参考满量程和校准表 | 纵轴准确性 |
| TBD-10 | Qt 目标显示后端：linuxfb、XCB、Wayland 或其他 | PetaLinux rootfs 和启动脚本 |
| TBD-11 | 实际屏幕接口、触摸设备和分辨率 | UI 自适应和输入方式 |
| TBD-12 | Linux AXI DMA 驱动的用户态 API | `DmaSpectrumSource` 实现 |

## 5. 总体技术约束

### 5.1 开发语言与构建系统

- 开发语言：C++17；
- GUI 框架：Qt Widgets；
- PC 阶段：Qt 6.x；
- 目标端：Qt 5.15.4 基线；
- 构建系统：CMake；
- 单元测试：Qt Test；
- 源代码编码：UTF-8；
- 默认界面语言：简体中文，频率和幅度单位采用国际通用缩写。

### 5.2 Qt 兼容要求

1. 业务代码不得依赖仅在 Qt 6 中存在且无 Qt 5 替代方案的 API。
2. Qt 5/Qt 6 API 差异必须封装在独立兼容层中，不得散布于全部业务代码。
3. CMake 应通过 `find_package(QT NAMES Qt6 Qt5 ...)` 支持双版本构建。
4. 不使用已在 Qt 6 移除的 `QRegExp` 等接口。
5. 鼠标坐标、编码、容器行为等差异应通过条件编译集中处理。
6. 每个阶段性版本至少在 Windows Qt 6 和 Qt 5.15.x 各完成一次构建验证。

### 5.3 无GPU渲染约束

1. 一期不得把OpenGL、OpenGL ES、EGL或GPU驱动作为运行前提，也不实现OpenGL渲染后端。
2. 不采用`QOpenGLWidget`、Qt Quick Scene Graph或软件OpenGL作为一期频谱绘制路径。
3. 默认采用继承自`QWidget`的自定义频谱控件和Qt Raster `QPainter`绘制。
4. 坐标轴、网格、标签等静态内容必须缓存，仅在窗口尺寸或坐标范围变化时重绘。
5. 高频频谱曲线必须按可见像素宽度做降采样后再绘制。
6. 降采样必须保留每个像素列覆盖频点的最小值和最大值，不能仅抽取单点，以免窄带峰值消失。
7. 绘制所用数组、图像和路径对象应预分配或复用，避免逐帧大量内存申请。
8. 软件架构可以保留抽象渲染接口，但GPU后端不属于一期实现和验收范围。

### 5.4 PetaLinux 与交叉编译约束

- PetaLinux 2023.2 标准 Qt5 发布层的配方基线为 Qt 5.15.4。
- Qt 通常不是最小 rootfs 的默认组件，目标工程需选择 `packagegroup-petalinux-qt`。
- 生成 Qt 交叉编译 SDK 时需按官方流程启用 `populate_sdk_qt5`。
- Windows 用于本机开发和模拟测试；最终 PetaLinux SDK 生成及 ARM64 交叉编译应在受支持的 Linux 主机环境中完成。
- Linux 构建主机优先采用 PetaLinux 2023.2 文档支持的 Ubuntu 20.04.6 LTS 或 Ubuntu 22.04.2 LTS。
- 目标可执行文件不得动态依赖未加入 PetaLinux rootfs 的第三方库。

## 6. 软件总体架构

### 6.1 分层结构

```text
┌──────────────────────────────────────────────────────────┐
│                 表示与交互层 Presentation                │
│ 主窗口、参数面板、状态栏、SpectrumPlotWidget、对话框      │
├──────────────────────────────────────────────────────────┤
│                   业务与测量层 Domain                    │
│ 轨迹模式、平均/保持、Marker、峰值搜索、单位和坐标映射      │
├──────────────────────────────────────────────────────────┤
│                    数据管线层 Pipeline                   │
│ 频谱帧校验、格式归一化、缓冲池、最新帧发布、统计           │
├──────────────────────────────────────────────────────────┤
│                   数据源抽象层 Sources                   │
│ SimulatedSpectrumSource │ DmaSpectrumSource（后续）       │
├──────────────────────────────────────────────────────────┤
│                    平台适配层 Platform                   │
│ Windows计时/文件系统 │ Linux驱动接口/QPA/部署             │
└──────────────────────────────────────────────────────────┘
```

### 6.2 核心模块

| 模块 | 主要职责 |
|---|---|
| Application | 进程初始化、命令行参数、全局异常处理和退出流程 |
| MainWindow | 主界面布局、菜单、工具栏、状态栏和功能协调 |
| SpectrumPlotWidget | 频谱画布、网格、坐标、Marker、交互及CPU Raster绘制 |
| RasterSpectrumRenderer | CPU Raster绘制、静态缓存和峰值保真包络渲染 |
| ISpectrumSource | 数据源统一接口、生命周期和状态通知 |
| SimulatedSpectrumSource | 生成可重复、可配置的模拟频谱数据 |
| DmaSpectrumSource | 后续封装 DMA 用户态 API，不承担内核驱动职责 |
| SpectrumPipeline | 帧校验、归一化、发布、丢帧策略和统计 |
| TraceProcessor | Clear/Write、平均、最大保持和最小保持 |
| MeasurementEngine | Marker、峰值、带宽和功率类测量 |
| RenderReducer | 按屏幕像素执行最小/最大包络降采样 |
| ConfigurationManager | 配置加载、校验、原子保存和默认值恢复 |
| ExportService | PNG 截图、CSV 频谱数据导出 |
| Diagnostics | 日志、性能计数器、异常状态和运行信息 |

### 6.3 线程模型

| 线程 | 职责 | 约束 |
|---|---|---|
| GUI 主线程 | 控件事件、布局和最终绘制 | 不得阻塞等待 DMA、文件或大批量计算 |
| 采集线程 | 模拟数据生成或 DMA 数据获取 | 使用预分配缓冲，不直接操作 GUI |
| 处理线程 | 帧解析、标度转换、轨迹处理和测量 | 输入过载时执行明确的丢帧策略 |
| 可选导出线程 | CSV 写入和较大截图保存 | 不得长时间阻塞 GUI |

一期可以根据实测将采集和处理合并为一个工作线程，但公共接口不得阻止后续拆分。

### 6.4 缓冲与数据所有权

1. 频谱缓冲在启动或参数变更时预分配，稳定运行期间不得逐帧频繁申请大块内存。
2. 采集、处理和显示之间使用双缓冲、三缓冲或固定容量 SPSC 环形队列。
3. 发布给 GUI 的频谱帧在绘制期间必须保持只读且生命周期有效。
4. GUI 默认获取“最新完整帧”，不得排队绘制大量已经过期的历史帧。
5. 必须分别统计：源产生帧、源接收帧、协议错误帧、处理丢弃帧、显示帧。
6. 不得用高频 Qt queued signal 逐帧携带大容量 `QVector<float>` 副本；应发布轻量帧句柄或共享只读缓冲。

## 7. 内部数据模型

### 7.1 统一频谱帧

内部 `SpectrumFrame` 至少包含：

| 字段 | 建议类型 | 说明 |
|---|---|---|
| sequence | `uint64_t` | 单调递增帧序号 |
| timestampNs | `uint64_t` | 采集或生成时间戳，单位 ns |
| centerFrequencyHz | `double` | 中心频率 |
| spanHz | `double` | 当前分析带宽 |
| startFrequencyHz | `double` | 起始频率，可由中心频率和 Span 推导 |
| stopFrequencyHz | `double` | 终止频率，可由中心频率和 Span 推导 |
| binCount | `uint32_t` | 有效频点数 |
| amplitudeUnit | 枚举 | dBFS、dBm、dBc 或 Linear |
| flags | 位标志 | 有效性、过载、校准状态等 |
| bins | 只读 `float32` 数组 | 按频率升序排列的显示值 |

### 7.2 频点频率映射

默认采用包含起点、不重复包含终点的映射：

```text
binWidth = spanHz / binCount
frequency(i) = startFrequencyHz + i × binWidth
```

若 PL 最终协议采用其他定义，应仅修改协议适配层和映射策略。

### 7.3 幅度归一化

- 一期模拟数据内部单位为 dBFS；
- 显示有效范围默认 `-160 dBFS ～ 0 dBFS`，用户可调整；
- 真实 DMA 数据通过 `displayValue = rawValue × scale + offset` 或协议指定公式转换；
- 未完成硬件标定前，界面必须明确标注“未校准”，不得把估算值展示为准确 dBm；
- NaN、Inf 和越界值必须被检测并按无效数据处理。

## 8. 功能需求

本节优先级定义：`MUST` 为一期必须实现，`SHOULD` 为一期建议实现，`COULD` 为后续扩展。

### 8.1 应用启动与退出

| 编号 | 优先级 | 需求 |
|---|---|---|
| FR-APP-001 | MUST | 软件应能够在 Windows Qt 6 环境启动模拟版。 |
| FR-APP-002 | MUST | 软件应能够使用 ARM64 Qt5 SDK完成交叉编译并在目标 Linux 启动。 |
| FR-APP-003 | MUST | 启动时应加载配置；配置不存在或损坏时使用安全默认值。 |
| FR-APP-004 | MUST | 支持通过命令行选择 `simulated` 或未来的 `dma` 数据源。 |
| FR-APP-005 | MUST | 退出时停止数据源、等待工作线程结束并释放资源，不得强制终止线程。 |
| FR-APP-006 | SHOULD | 支持全屏启动及开机自启所需的命令行选项。 |

### 8.2 模拟数据源

| 编号 | 优先级 | 需求 |
|---|---|---|
| FR-SIM-001 | MUST | 支持白噪声底，噪声电平和随机波动可配置。 |
| FR-SIM-002 | MUST | 支持一个或多个固定频率信号，每个信号的频率、幅度和等效带宽可配置。 |
| FR-SIM-003 | MUST | 支持扫频信号，起止频率、扫频周期、方向和幅度可配置。 |
| FR-SIM-004 | MUST | 支持瞬态信号，出现概率、持续时间和幅度可配置。 |
| FR-SIM-005 | MUST | FFT 点数可在 1,024～65,536 范围内配置，并至少支持2的整数次幂。 |
| FR-SIM-006 | MUST | 输出帧率可配置，并支持无等待的性能压测模式。 |
| FR-SIM-007 | MUST | 支持固定随机种子，使测试场景可重复。 |
| FR-SIM-008 | SHOULD | 可模拟数据暂停、帧序号跳变、异常帧和突发过载。 |
| FR-SIM-009 | SHOULD | 信号合成应在线性功率域完成后再转换为 dB，避免直接叠加 dB 值造成不真实结果。 |
| FR-SIM-010 | SHOULD | 支持通过 JSON 场景文件保存和载入模拟信号组合。 |

### 8.3 数据源控制与状态

| 编号 | 优先级 | 需求 |
|---|---|---|
| FR-SRC-001 | MUST | 数据源具有 Initialized、Running、Paused、Stopped、Error 状态。 |
| FR-SRC-002 | MUST | 用户能够启动、暂停、继续和停止数据源。 |
| FR-SRC-003 | MUST | 状态变化和错误信息应以轻量事件通知业务层。 |
| FR-SRC-004 | MUST | 数据源切换不得要求重建频谱显示控件。 |
| FR-SRC-005 | MUST | 参数变更造成帧尺寸改变时，必须安全地重建缓冲区并避免使用旧帧。 |
| FR-SRC-006 | SHOULD | 可配置自动重试次数和重试间隔。 |

### 8.4 实时频谱显示

| 编号 | 优先级 | 需求 |
|---|---|---|
| FR-DSP-001 | MUST | 显示实时频谱曲线、坐标轴、主/次网格和单位。 |
| FR-DSP-002 | MUST | 横轴支持 Start/Stop 及 Center/Span 两种等价参数表达。 |
| FR-DSP-003 | MUST | 纵轴支持参考电平、底部电平、每格刻度和自动范围。 |
| FR-DSP-004 | MUST | 坐标标签应自动选择 Hz、kHz、MHz、GHz 以及 dBFS/dBm 等单位。 |
| FR-DSP-005 | MUST | 绘图采用 CPU Raster 路径，不要求 GPU。 |
| FR-DSP-006 | MUST | 频点多于可见像素宽度时，使用每像素列最小/最大包络降采样。 |
| FR-DSP-007 | MUST | 降采样后的绘制点数应与画布宽度同阶，建议不超过 `2 × plotWidth` 个幅度端点。 |
| FR-DSP-008 | MUST | 网格和坐标缓存不得在每个输入帧中无条件重建。 |
| FR-DSP-009 | MUST | 窗口缩放后频谱和文本清晰，无越界和明显闪烁。 |
| FR-DSP-010 | MUST | 支持暂停显示，暂停时保留最后一帧和测量结果。 |
| FR-DSP-011 | MUST | 支持复位缩放和恢复完整频率范围。 |
| FR-DSP-012 | SHOULD | 支持鼠标滚轮缩放、拖动平移和框选缩放。 |
| FR-DSP-013 | SHOULD | 支持鼠标和触摸操作的基本兼容。 |
| FR-DSP-014 | SHOULD | 支持曲线颜色、线宽、网格和主题配置。 |

### 8.5 轨迹模式

| 编号 | 优先级 | 需求 |
|---|---|---|
| FR-TRACE-001 | MUST | 支持 Clear/Write 实时覆盖模式。 |
| FR-TRACE-002 | MUST | 支持最大保持 Max Hold，并可清除历史结果。 |
| FR-TRACE-003 | MUST | 支持平均 Average，平均次数或平滑系数可配置。 |
| FR-TRACE-004 | SHOULD | 支持最小保持 Min Hold。 |
| FR-TRACE-005 | MUST | FFT 点数、频率范围或单位变化后自动重置不兼容的历史轨迹。 |
| FR-TRACE-006 | MUST | 轨迹处理不得修改数据源提供的原始只读帧。 |

### 8.6 Marker 与峰值搜索

| 编号 | 优先级 | 需求 |
|---|---|---|
| FR-MKR-001 | MUST | 至少支持一个 Marker，显示频率和幅度。 |
| FR-MKR-002 | MUST | 支持 Peak Search，将 Marker 移动到当前轨迹最大值。 |
| FR-MKR-003 | SHOULD | 支持最多四个 Marker。 |
| FR-MKR-004 | SHOULD | 支持 Delta Marker，显示相对频差和幅度差。 |
| FR-MKR-005 | SHOULD | 支持 Next Peak、Previous Peak 和峰值门限设置。 |
| FR-MKR-006 | MUST | Marker 测量基于原始或轨迹频点数据，不能基于屏幕降采样结果。 |
| FR-MKR-007 | MUST | Marker 在缩放、平移和窗口变化后保持与频率数据的一致映射。 |

### 8.7 常用测量

| 编号 | 优先级 | 需求 |
|---|---|---|
| FR-MEAS-001 | SHOULD | 支持用户指定频率区间内的峰值频率和峰值幅度。 |
| FR-MEAS-002 | SHOULD | 支持指定区间内的积分功率/信道功率；在未标定前以相对值或 dBFS 显示。 |
| FR-MEAS-003 | COULD | 支持占用带宽 OBW 测量。 |
| FR-MEAS-004 | COULD | 支持 -3 dB、-6 dB 等相对带宽测量。 |
| FR-MEAS-005 | MUST | 测量结果必须标注单位、有效状态和是否已经校准。 |

### 8.8 参数面板

一期参数面板至少包含：

- 数据源选择；
- 中心频率、Span、Start、Stop；
- FFT 点数；
- 模拟帧率；
- 参考电平、纵轴底部、每格刻度；
- 轨迹模式及平均参数；
- 模拟噪声和信号参数；
- 开始、暂停、继续、单次和停止操作。

输入要求：

1. 所有数值输入必须校验范围和单位。
2. 支持 `1000`、`1 kHz`、`10 MHz` 等明确形式；若一期未实现带单位文本解析，则界面必须通过下拉框明确单位。
3. 非法值不得传入数据源，界面应给出具体错误原因。
4. 相互关联的 Center/Span 与 Start/Stop 参数必须保持一致。

### 8.9 状态栏与诊断

| 编号 | 优先级 | 需求 |
|---|---|---|
| FR-STAT-001 | MUST | 显示数据源状态和运行时间。 |
| FR-STAT-002 | MUST | 显示输入帧率、GUI 绘制帧率和当前 FFT 点数。 |
| FR-STAT-003 | MUST | 显示平均数据率、累计接收帧和累计丢弃帧。 |
| FR-STAT-004 | MUST | 显示最后错误和错误发生时间。 |
| FR-STAT-005 | SHOULD | 显示处理耗时、绘制耗时、队列深度和端到端延迟统计。 |
| FR-STAT-006 | MUST | 统计信息不得通过全局高频锁显著影响采集性能。 |

### 8.10 截图、导出与配置

| 编号 | 优先级 | 需求 |
|---|---|---|
| FR-IO-001 | MUST | 支持将当前界面或频谱绘图区导出为 PNG。 |
| FR-IO-002 | MUST | 支持将当前完整频谱帧导出为 CSV，而非导出降采样显示数据。 |
| FR-IO-003 | MUST | CSV 至少包含帧元数据、频点序号、频率、幅度和单位。 |
| FR-IO-004 | MUST | 支持保存和恢复界面、显示、轨迹和模拟数据参数。 |
| FR-IO-005 | MUST | 配置写入应使用临时文件加原子替换，避免掉电产生半写文件。 |
| FR-IO-006 | MUST | 配置文件版本不兼容时应回退到默认值并记录日志。 |
| FR-IO-007 | SHOULD | 大容量 CSV 写入应在后台执行并显示进度或完成提示。 |

### 8.11 日志与异常处理

- 日志至少包含时间、级别、模块、事件和错误原因；
- 支持 Debug、Info、Warning、Error 级别；
- 日志文件应限制单文件大小和保留数量；
- DMA 超时、帧长度错误、帧序号跳变、数据中断和解析失败应分类统计；
- 可恢复错误应自动重试且不阻塞 GUI；
- 不可恢复错误应停止相应数据源，保留最后有效频谱并允许用户重启；
- 任何异常不得导致无界内存增长或高频错误日志淹没存储空间。

## 9. AXI DMA 后续接入要求

### 9.1 数据源接口要求

`DmaSpectrumSource` 应实现与模拟数据源相同的生命周期和帧发布接口。其职责仅包括：

- 打开和关闭用户态 DMA 设备；
- 获取已完成的 DMA 缓冲；
- 识别帧边界并校验帧长度；
- 将原始频点转换为内部 `float32` 频谱帧；
- 维护帧序号、数据率、超时和错误统计；
- 在停止或错误时安全归还 DMA 缓冲。

内核驱动、中断、DMA 映射、缓存一致性和设备树由硬件/驱动提供方负责，但必须向本软件提供稳定的用户态 API 文档。

### 9.2 推荐的 PL/PS 协议原则

以下为建议而非当前已确认协议：

1. 一帧对应一次完整 FFT 结果，禁止把两个帧无标识地拼接。
2. 使用 `TLAST` 或固定长度明确帧边界。
3. 帧长度按 AXI 总线宽度和缓存行要求对齐。
4. 至少提供帧序号，推荐同时提供硬件时间戳。
5. 中心频率、Span、FFT 点数、数据格式和标度信息可位于帧头，或通过版本化寄存器组提供。
6. 推荐提供协议版本号、载荷长度和有效标志。
7. 若使用定点数，必须明确总位宽、小数位数、符号、饱和方式和字节序。
8. 必须说明 FFT Shift 是否已经完成，以及 DC 对应的频点索引。
9. 不建议在每个频点中重复附带频率；频率应由元数据和索引计算。
10. 可选 CRC 适用于需要检测内存/链路数据损坏的场景，但需评估计算开销。

### 9.3 两种可接受的传输形式

**形式 A：固定长度裸载荷**

- 每帧固定 FFT 点数；
- `TLAST` 标记帧末尾；
- 元数据通过 AXI-Lite 寄存器或软件配置获得；
- 开销低，适合固定配置和极高数据率。

**形式 B：版本化帧头加载荷**

- 帧头包含 Magic、协议版本、帧序号、时间戳、频点数、载荷类型、标度和载荷长度；
- 便于动态切换 FFT 点数和长期扩展；
- 帧头建议固定大小并按总线宽度对齐。

协议最终确定后，应新增独立《PL/PS 频谱数据接口协议》，本文只引用其版本。

### 9.4 缓存一致性要求

- 驱动必须明确 DMA 缓冲是否为 coherent；
- 若非一致性内存，驱动必须在缓冲所有权切换时完成正确的 cache sync；
- 用户态不得自行假定普通 `malloc` 内存可直接用于 DMA；
- 软件不得在 DMA 正在写入时读取同一缓冲；
- DMA 缓冲所有权必须具有明确的“空闲、硬件占用、用户态只读、归还”状态。

## 10. UI 与交互设计要求

### 10.1 总体布局

1920×1080 默认布局建议：

```text
┌──────────────────────────────────────────────────────────────┐
│ 菜单栏 / 快捷工具栏                                           │
├───────────────────────────────────────────────┬──────────────┤
│                                               │ 数据源与参数  │
│                                               │ Center/Span  │
│                实时频谱绘图区                  │ 幅度与轨迹    │
│                                               │ Marker/测量  │
│                                               │ 模拟信号      │
├───────────────────────────────────────────────┴──────────────┤
│ 状态、输入FPS、显示FPS、数据率、丢帧、延迟、错误               │
└──────────────────────────────────────────────────────────────┘
```

### 10.2 视觉要求

- 默认深色仪器主题；
- 频谱曲线与网格具有足够对比度；
- 重要告警不得只依赖颜色区分；
- 关键数值在 1920×1080 下清晰可读；
- UI 缩放不得使用写死的绝对坐标；
- 频率、幅度和状态文本不得因数值变长而遮挡主要绘图区；
- 支持窗口模式和全屏模式。

### 10.3 通用绘制优化策略

1. 将画布横向像素列映射到频点区间，对区间求最小值和最大值。
2. 每个像素列保留最小—最大包络，避免窄带峰值在降采样时消失。
3. 当频点数少于像素宽度时直接按频点绘制折线。
4. 降采样坐标数组和帧缓冲应预分配并复用。
5. GUI使用固定刷新定时器，输入帧到来只更新“最新帧句柄”，不为每帧立即触发重绘。
6. 刷新周期内收到多个帧时，只绘制最新完整帧，同时保留接收统计。
7. Marker和测量必须使用完整频点数据，确保绘制优化不影响测量准确性。

### 10.4 CPU Raster 绘制策略

1. `SpectrumPlotWidget` 采用 Raster `QPainter`。
2. 背景、边框、网格和坐标文本缓存到离屏 `QPixmap` 或 `QImage`。
3. 每次新帧只更新曲线、Marker 和必要的动态文本。
4. 对每个像素列绘制最小—最大垂直包络，或构造有序包络折线。
5. 图像格式优先采用目标平台原生且适合Raster引擎的格式，避免逐帧像素格式转换。
6. 应减少整个窗口的无效重绘，将高频更新区域限制在频谱画布内。

## 11. 性能需求与数据量估算

### 11.1 数据量公式

若每个频点在软件内部使用 4 字节 `float32`：

```text
单帧载荷字节数 = FFT频点数 × 4
持续数据率 = 单帧载荷字节数 × 输入帧率
```

不含帧头、对齐和驱动额外开销的典型数据量如下：

| 频点数 | 单帧载荷 | 200 帧/s | 1,000 帧/s |
|---:|---:|---:|---:|
| 1,024 | 4 KiB | 0.82 MB/s | 4.10 MB/s |
| 4,096 | 16 KiB | 3.28 MB/s | 16.38 MB/s |
| 8,192 | 32 KiB | 6.55 MB/s | 32.77 MB/s |
| 16,384 | 64 KiB | 13.11 MB/s | 65.54 MB/s |
| 32,768 | 128 KiB | 26.21 MB/s | 131.07 MB/s |
| 65,536 | 256 KiB | 52.43 MB/s | 262.14 MB/s |

若 PL 使用 16 位频点，则 DMA 原始数据率为表中一半；若使用 64 位频点，则为表中两倍。最终以实际协议为准。

### 11.2 性能指标分类

| 编号 | 类别 | 指标 |
|---|---|---|
| NFR-PERF-001 | MUST | 支持 1,024～65,536 频点的参数化数据结构和显示。 |
| NFR-PERF-002 | MUST | 1920×1080 下稳定达到不低于 30 FPS 的 GUI 刷新率。 |
| NFR-PERF-003 | TARGET | 在典型目标配置下达到 60 FPS GUI 刷新率。 |
| NFR-PERF-004 | MUST | 输入帧率与显示帧率解耦，输入 1,000 帧/s 时不得产生 1,000 次 GUI 重绘。 |
| NFR-PERF-005 | MUST | PC 模拟环境支持 16,384 点、1,000 帧/s 数据生成和处理，GUI 保持不少于30 FPS。 |
| NFR-PERF-006 | TARGET | 目标板支持 16,384 点、1,000 帧/s，即约65.54 MB/s内部 `float32` 数据流。 |
| NFR-PERF-007 | TARGET | 目标板支持65,536点、200帧/s，即约52.43 MB/s内部数据流。 |
| NFR-PERF-008 | STRETCH | 65,536点、1,000帧/s约262.14 MB/s作为极限压测项，是否保证由DMA、DDR、CPU和协议联调决定。 |
| NFR-PERF-009 | MUST | UI 可见数据延迟的95百分位不超过150 ms；典型目标不超过100 ms。 |
| NFR-PERF-010 | MUST | 渲染降采样复杂度为 O(N)，最终绘制点数量为 O(屏幕宽度)。 |
| NFR-PERF-011 | MUST | GUI 线程单次绘制不得等待新数据或持有采集侧互斥锁。 |
| NFR-PERF-012 | MUST | 目标板性能测试必须使用CPU Raster路径，不得依赖软件OpenGL绕过无GPU约束。 |

### 11.3 资源指标

以下为初始目标，板端联调后允许形成新的性能基线：

- 典型配置下进程常驻内存目标不超过 256 MiB；
- 固定配置连续运行时内存不得随帧数线性增长；
- 连续24小时测试后，进程内存增长应不超过10 MiB且不存在持续增长趋势；
- 日志和导出文件不得占满系统分区；
- 软件不应长期独占全部 CPU 核心，典型 16,384点/60 FPS场景的CPU占用应在目标板实测后设置正式上限；
- 当CPU负载过高时优先降低显示刷新率或丢弃过期显示帧，不允许阻塞采集导致无界排队。

### 11.4 过载策略

1. 采集层尽可能接收完整帧并维护源侧统计。
2. 处理队列满时丢弃最旧的待处理帧，保留最新帧。
3. GUI 仅绘制刷新时刻的最新已处理帧。
4. 最大保持和平均模式是否允许跳过处理帧必须可配置：
   - “显示优先”模式允许跳帧；
   - “测量完整性优先”模式要求处理每帧，过载时降低源速率或报告能力不足。
5. 所有丢帧均应可见、可统计，不得静默发生。

## 12. 非功能需求

### 12.1 可靠性

- 软件应支持连续运行24小时以上；
- 模拟数据源异常不得导致主进程崩溃；
- 数据中断后界面保持响应；
- 恢复数据后能够继续显示或经一次明确重启操作恢复；
- 参数快速连续修改不得产生悬空指针或线程竞争；
- 所有跨线程对象应具有明确的创建、使用和销毁线程归属。

### 12.2 可维护性

- 模块职责清晰，不把数据生成、DMA、轨迹计算和绘制写入同一个窗口类；
- 公共接口使用版本化数据结构；
- 核心类和复杂算法具有必要注释；
- 配置项和常量集中管理；
- 错误码与日志信息可追踪到具体模块；
- 格式转换器、降采样器和测量引擎可脱离 GUI 单元测试。

### 12.3 可扩展性

- 新增瀑布图时复用统一频谱帧发布机制；
- 支持未来一个数据源发布多个逻辑通道；
- 绘制后端可扩展为 CPU Raster 和可选 GPU 两种实现；
- 协议解析器与 DMA 设备访问分离，协议变化不要求修改驱动封装；
- 测量功能通过独立接口注册，不直接耦合主窗口。

### 12.4 可移植性

- 禁止在业务层直接包含 Windows 或 Linux 专用头文件；
- 平台相关实现放入独立目录并通过接口隔离；
- 文件路径使用 Qt 跨平台接口；
- 不假设目标端存在桌面环境；
- 应支持至少一种无GPU的目标QPA后端，优先验证`linuxfb`，若硬件显示栈提供X11则同时验证`xcb`。

### 12.5 安全性

- 配置文件和场景文件必须校验类型、范围和数组大小；
- 不信任 DMA 帧头中的长度字段，解析前必须与实际缓冲长度比较；
- 文件导出不得无提示覆盖已有文件；
- 日志不得记录敏感凭据；
- 数据长度计算必须防止整数溢出和越界访问。

## 13. 配置文件建议

配置采用带版本号的 JSON 或 INI。建议至少包含：

```text
schemaVersion
application.language
application.theme
application.fullscreen
display.refreshFps
display.referenceLevel
display.bottomLevel
display.gridDivision
display.traceMode
display.averageCount
source.type
source.simulated.binCount
source.simulated.frameRate
source.simulated.centerFrequencyHz
source.simulated.spanHz
source.simulated.noiseFloorDbfs
source.simulated.seed
source.dma.devicePath
source.dma.timeoutMs
logging.level
logging.maxFileSize
logging.retainedFiles
```

设备路径和目标端专用参数不得硬编码在源代码中。

## 14. 测试需求

### 14.1 单元测试

至少覆盖：

- 频率与频点索引双向转换；
- Center/Span 与 Start/Stop 转换；
- dB/线性功率转换；
- 最小/最大包络降采样，尤其是单频点窄峰保留；
- 平均、最大保持和最小保持；
- Peak Search 和 Marker；
- 配置文件校验和版本迁移；
- 帧长度、异常数值和整数溢出检查；
- 固定随机种子模拟场景的可重复性。

### 14.2 PC 集成测试

| 测试项 | 初始验收要求 |
|---|---|
| 基本显示 | 模拟噪声和信号峰正常显示，坐标与参数一致 |
| 交互 | 缩放、平移、Marker、暂停和参数修改无异常 |
| 轨迹模式 | Clear/Write、Average、Max Hold 结果符合预期 |
| 动态点数 | 1,024、4,096、16,384、65,536点切换稳定 |
| 性能 | 16,384点、1,000帧/s输入时GUI不少于30 FPS |
| 峰值保留 | 65,536点数据中的单点或窄带峰经降采样后仍可见 |
| 过载 | 输入超过处理能力时界面响应正常且丢帧计数增加 |
| 长稳 | 连续运行24小时无崩溃、死锁和持续内存增长 |
| 导出 | PNG与CSV内容正确，CSV包含完整频点 |
| 双Qt版本 | Qt 6.x和Qt 5.15.x均能构建和运行核心功能 |

### 14.3 目标板测试

- Qt 应用可在选定 QPA 平台启动；
- 1920×1080 下布局正确；
- 验证程序使用Qt Raster路径，未加载OpenGL频谱渲染后端；
- 无GPU和OpenGL驱动时功能完整；
- 模拟数据模式下达到至少30 FPS；
- CPU、内存、温度和调度延迟在可接受范围；
- 运行24小时无崩溃和持续资源泄漏；
- ARM64部署包所需动态库全部存在；
- 关机或异常退出后配置文件仍可正确读取。

### 14.4 DMA 联调测试

真实硬件就绪后增加：

- 固定测试向量逐点比对；
- 帧长、帧序号、`TLAST`和字节序校验；
- 不同FFT点数切换；
- 最大持续数据率和突发数据率；
- DMA超时、复位和自动恢复；
- 缓存一致性；
- PL输入已知单音时频率和幅度误差；
- 从PL产生帧到屏幕显示的端到端延迟；
- 真实数据连续24小时稳定性。

## 15. 一期验收标准

一期完成需同时满足：

1. Windows PC 上存在可运行的 Qt 模拟版。
2. 支持 Qt 6.x 和 Qt 5.15.x 构建。
3. 模拟数据源支持噪声、固定信号、多信号、扫频和瞬态场景。
4. 支持 1,024～65,536点频谱数据结构及显示。
5. 1920×1080、16,384点、1,000帧/s模拟输入时GUI达到至少30 FPS。
6. 软件在不使用GPU的条件下能够完整运行，窄带峰值不会因绘制降采样消失。
7. 实现实时频谱、坐标参数、Marker、Peak Search、Average和Max Hold。
8. 实现开始、暂停、继续、停止及基本缩放操作。
9. 实现状态、输入帧率、绘制帧率、数据率和丢帧统计。
10. 实现 PNG 截图、完整频谱 CSV 导出及配置保存恢复。
11. 连续运行24小时无崩溃、死锁和持续内存增长。
12. 数据源接口允许后续新增 `DmaSpectrumSource`，无需重写显示模块。
13. 完成 ARM64交叉编译说明和目标板模拟模式运行验证。

真实DMA吞吐率、幅度准确度和硬件端到端延迟在PL协议及驱动完成前不纳入一期最终验收。

## 16. 建议工程目录

```text
RTSA/
├─ CMakeLists.txt
├─ cmake/
├─ config/
├─ docs/
├─ resources/
├─ src/
│  ├─ app/
│  ├─ ui/
│  ├─ plot/
│  ├─ domain/
│  ├─ pipeline/
│  ├─ sources/
│  │  ├─ simulated/
│  │  └─ dma/
│  ├─ platform/
│  │  ├─ windows/
│  │  └─ linux/
│  ├─ compatibility/
│  └─ diagnostics/
├─ tests/
│  ├─ unit/
│  ├─ integration/
│  └─ performance/
└─ tools/
```

## 17. 开发阶段建议

### 阶段一：工程骨架与数据模型

- 建立 CMake Qt5/Qt6 双版本工程；
- 定义 `SpectrumFrame`、`ISpectrumSource` 和状态模型；
- 实现基础日志、配置及测试框架。

### 阶段二：模拟数据与基础频谱

- 实现模拟数据源；
- 实现无 GPU 的 `SpectrumPlotWidget`；
- 实现坐标、网格、Center/Span及纵轴控制。

### 阶段三：性能管线与仪器功能

- 实现预分配缓冲和最新帧发布；
- 实现峰值保真的包络降采样；
- 实现轨迹、Marker、Peak Search和状态统计；
- 完成高帧率压力测试。

### 阶段四：数据管理与稳定性

- 实现配置、截图和CSV；
- 完成长稳、异常、过载和内存测试；
- 优化CPU占用和绘制耗时。

### 阶段五：Qt5与目标板部署

- 在Windows Qt 5.15.x验证兼容性；
- 在Linux主机生成PetaLinux Qt5 SDK；
- 交叉编译并在目标板模拟模式运行；
- 确认QPA显示后端、字体、输入设备及部署依赖。

### 阶段六：DMA 接入与硬件联调

- 获取正式PL/PS协议和DMA用户态API；
- 实现 `DmaSpectrumSource` 和格式转换器；
- 完成吞吐、延迟、丢帧、标定和长稳测试。

## 18. 交付物

- 完整 Qt/C++源代码；
- CMake构建文件；
- Windows模拟版可执行程序；
- 模拟场景及默认配置；
- 单元测试、集成测试和性能测试；
- PetaLinux Qt5 SDK生成说明；
- ARM64交叉编译和部署说明；
- 软件用户操作说明；
- DMA数据源接口说明；
- 测试报告和已知问题清单；
- 后续PL/PS协议确认清单。

## 19. 风险与应对

| 风险 | 影响 | 应对措施 |
|---|---|---|
| Windows Qt6与目标Qt5存在API差异 | 后期移植返工 | 双版本CMake、兼容层、尽早安装Qt 5.15.x持续构建 |
| 目标板无可用GPU | 高频绘图CPU占用高 | Raster自绘、静态缓存、像素包络降采样、固定刷新率和按需NEON优化 |
| PL协议晚确定 | DMA接入延期 | 统一内部帧和数据源接口，协议解析独立 |
| DMA峰值数据率过高 | 丢帧或延迟累积 | 预分配缓冲、零/少拷贝、最新帧策略、明确过载模式 |
| 幅度标度不明确 | 显示数值不可信 | 一期使用dBFS并标记未校准，后续加载标定公式 |
| Qt未加入rootfs | 目标程序无法运行 | 选择Qt packagegroup并生成Qt5 SDK，部署前检查动态依赖 |
| 显示后端未知 | 板端界面启动失败 | 尽早验证linuxfb/xcb及字体、输入设备和屏幕驱动 |
| 软件渲染撕裂或刷新不足 | 显示体验下降 | 控制刷新率、减少无效重绘、验证QPA后端双缓冲能力 |

## 20. 参考资料

- AMD PetaLinux Tools Documentation: Reference Guide (UG1144), 2023.2：<https://docs.amd.com/r/2023.2-English/ug1144-petalinux-tools-reference-guide>
- Xilinx `meta-qt5` PetaLinux 2023.2分支：<https://github.com/Xilinx/meta-qt5/tree/rel-v2023.2>
- Qt5配方版本定义（`PV = 5.15.4+git${SRCPV}`）：<https://github.com/Xilinx/meta-qt5/blob/rel-v2023.2/recipes-qt/qt5/qt5-git.inc>

## 21. 需求基线说明

本文档以当前已知信息形成初始基线。以下事件发生时应更新文档版本：

- PL输出数据协议确定；
- AXI DMA模式和Linux驱动API确定；
- 频点数、帧率和数据位宽确定；
- 目标显示后端和屏幕硬件确定；
- 幅度标定公式确定；
- 一期功能优先级或验收指标发生变化。

所有待确认参数在正式硬件联调前均不得以猜测值固化到代码中，应通过配置、协议元数据或适配层实现。
