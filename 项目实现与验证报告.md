# RFSoC ZU47DR 实时频谱分析仪项目实现与验证报告

## 1. 报告范围

本文记录当前 PC 模拟版的实际实现状态、测试证据、性能数据和后续阶段工作。需求基线见《实时频谱分析仪软件需求规格说明书》，实现基线见《实时频谱分析仪软件总体实现架构与设计方案》。

当前阶段的验收范围仅为：在 Windows PC 上完成 Qt 上位机，并使用内置模拟数据验证频谱能够正确、稳定显示。该范围已经完成并通过验收。PetaLinux/Qt 5 交叉编译、ZU47DR 板端运行、真实 AXI DMA 接入及板端性能验证均按项目安排进入后续阶段，不构成当前阶段的完成阻塞。

## 2. 已实现阶段

| 阶段 | 实现内容 | 状态 |
|---|---|---|
| A 基础工程 | Qt5/Qt6 CMake、C++17、核心帧模型、频率映射、包络和 Qt Test | 完成 |
| B 模拟数据管线 | 预分配帧池、统一数据源契约、噪声/固定信号/扫频/瞬态、限速与无等待、四类故障注入、JSON v1/v2 场景和统计 | 完成 |
| C CPU 频谱绘制 | Qt Widgets Raster 绘制、静态层缓存、峰值保真包络、完整频点 Marker 测量 | 完成 |
| D 仪器功能 | Center/Span、幅度/外观、轨迹、四 Marker/Delta/相邻峰、区间测量、鼠标/触摸交互、采集控制和全屏 | 完成 |
| E 导出与稳定性 | v4 设置迁移、PNG、异步 CSV、原子提交、滚动日志、异常恢复、延迟 P95、压力与应用冒烟 | PC 实现与验证完成 |
| F PetaLinux 部署 | 交叉编译和 linuxfb 运行步骤 | 后续阶段；文档已准备 |
| G DMA 联调 | `ISpectrumSource` 接口与统一 `SpectrumFrame` 已预留 | 后续阶段；等待 PL 协议及 Linux DMA 用户态 API |

## 3. 关键实现证据

### 3.1 无 GPU 路径

- 频谱控件为 `QWidget`；
- 最终绘制使用 `QPainter` Raster 后端；
- 默认关闭抗锯齿；
- 工程未链接 OpenGL、Qt Quick 或 Vulkan；
- 65,536 点先按屏幕列计算最小/最大包络，避免窄峰丢失。

### 3.2 实时数据路径

- `FramePool` 限制源帧数量并复用容量；
- `ISpectrumSource` 隔离模拟源和未来 DMA 源；
- 采集线程只向 `SpectrumPipeline` 发布完整帧，不操作 GUI；
- `LatestFrameStore` 使用原子 `shared_ptr` 发布，只保留最新完整状态；
- GUI 使用 16 ms 精确定时器刷新，不为每个输入帧排队；
- 流水线为成功发布帧分配独立单调序号，GUI 跳帧统计不受源序号跳变影响；
- 模拟源可按 1～1,000 FPS 节流，也可进入无等待压测模式；
- Marker、Peak Search 和 CSV 始终使用完整频点，不使用绘制包络。
- 区间峰值和信道功率使用独立 `SpectrumMeasurements`；dB 功率在线性域积分；
- 对应帧真正完成 `paintEvent()` 后写入固定 256 样本延迟窗口，低频计算 P95，不形成无界历史；

### 3.3 文件与配置

- PNG 从 Raster 频谱控件抓取；
- CSV 在 QtConcurrent 工作线程写入；
- 导出开始时固定不可变帧快照；
- `QSaveFile` 保证成功时原子替换，失败时不留下半成品目标文件；
- `QSettings` 保存窗口、频率、频点数、限速/无等待模式、完整模拟参数、幅度和轨迹设置；保存入口显式启用 Qt 原子同步，目标目录不支持临时文件和原子替换时返回失败；
- 设置快照当前为 v4，非法快照整体回退，兼容无版本、v1、v2 和 v3 配置迁移；
- JSON 模拟场景当前为 v2，兼容 v1，限制 1 MiB 和两个固定信号，严格校验类型、范围、随机种子、数组上限、半开频率边界、扫频区间及故障参数；
- 当前 UI 参数可通过 `QSaveFile` 原子保存为 JSON，写前由同一加载器验证，保存后重新加载往返测试通过；
- 交付默认、16,384 点无等待压测和确定性故障注入三个场景，安装到 `share/rtsa/config`；
- CSV 表头和元数据随 dBFS、dBm、dBc、Linear 单位及标定状态变化。

### 3.4 稳定性处理

- 运行中配置更新由互斥保护；
- 随机数状态只由采集工作线程访问，避免配置/生成数据竞争；
- 低帧率等待可被 Stop 唤醒，高帧率使用高分辨率调度；
- `pause()`、`resume()` 和 `stop()` 在与条件变量等待相同的互斥锁下更新谓词，避免检查谓词与进入等待之间丢失唤醒；
- 工作线程异常转换为 `Error` 状态，可清理并重新启动；
- 数据暂停等待可被 Stop/暂停/配置变化唤醒，序号跳变、NaN 异常帧和突发帧均可重复；
- 暂停/恢复使用条件状态转换，不会覆盖并发到达的 Error/Stopped 终止状态；
- 场景文件在读取内容前检查文件大小，并把单次读取限制在 1 MiB 边界内；
- 数据源创建后为 `Initialized`，启动时经 `Starting` 进入 `Running`，并覆盖 `Paused`、`Stopped`、`Error` 生命周期；
- 主窗口退出时停止并回收采集线程，等待尚未完成的 CSV 导出。

暂停/恢复测试已强化为连续 50 次状态切换；加入条件状态转换和丢帧分类测试后的 Release `rtsa_source_tests` 连续执行 20 轮（89.34 s）全部通过，未再出现 `stop()` 卡在 `join()` 的故障。GUI 性能测试改为首帧后的稳态窗口后，完整 `rtsa_gui_tests` 连续执行 10 轮全部通过，最低 1920×1080 显示采样仍为 33.46 FPS。加入旧备份超限清理后，包含 Qt 消息处理器并发卸载的 Release `rtsa_logging_tests` 连续执行 20 轮（20.83 s）也全部通过。

### 3.5 运行日志

- 日志使用 UTC 时间、级别、模块、事件和原因字段；
- 支持 Debug/Info/Warning/Error 过滤、字段清洗和 Qt 消息处理器桥接；
- 单文件大小与保留数量有界，避免持续占用系统分区；
- 多线程写入由互斥保护，每条记录保持完整行；
- Qt 消息处理器注册由静态互斥保护，停止时等待已进入的处理调用完成再析构；
- 单条超限记录带 `[truncated]` 标记截断，仍严格服从单文件大小上限；
- CTest 冒烟通过 `--no-file-log` 避免污染用户目录。

### 3.6 构建与测试可复现性

- Windows CTest 自动把当前 Qt 与 MinGW 运行库目录加入测试进程 `PATH`；
- GUI、绘图和应用冒烟测试固定使用 `QT_QPA_PLATFORM=offscreen`；
- 所有 CTest 项均设置 10～15 秒超时，避免 CI 无期限等待；
- Release 使用 `-Wall -Wextra -Wpedantic -Werror` 作为编译门禁。

## 4. 自动测试矩阵

| 测试程序 | 覆盖内容 |
|---|---|
| `rtsa_core_tests` | 频率映射、包络、轨迹、帧池、有界复用、非法帧、幅度单位、区间峰值/信道功率和处理遥测 |
| `rtsa_source_tests` | 生命周期、模拟信号、固定种子、线性功率合成、扫频、暂停/单次、限速/无等待、四类故障注入、错误恢复和遥测 |
| `rtsa_plot_tests` | 无 OpenGL Raster、峰值包络、四 Marker/Delta/门限/相邻峰、鼠标/框选、Pinch/Pan 信号、绘制完成信号和 65,536 点性能 |
| `rtsa_export_tests` | 完整 CSV、动态单位/标定、非有限值拒绝、无残缺失败文件 |
| `rtsa_settings_tests` | 安全默认值、原子同步、v4 外观往返、无版本/v1～v3 迁移、损坏/未来版本回退 |
| `rtsa_logging_tests` | 级别过滤、字段清洗、有界轮转、超长记录截断、4 线程完整行、Qt 消息桥接和并发卸载 |
| `rtsa_scenario_tests` | 三个交付场景、v1 迁移、v2 故障往返、原子保存和严格拒绝路径 |
| `rtsa_gui_tests` | 频率/幅度、四 Marker、区间测量、外观、诊断/P95、单次、全屏和全高清性能 |
| `rtsa_app_smoke` | 默认完整主窗口、自动采集、事件循环和正常退出 |
| `rtsa_app_scenario_smoke` | 加载默认 JSON 场景后的完整应用启动、采集和退出 |
| `rtsa_app_fault_scenario_smoke` | 组合暂停、序号跳变、异常帧和突发帧后的应用运行与退出 |
| `rtsa_app_invalid_source` 等 3 项 | 无效源、DMA/场景冲突和场景读取失败的精确退出码 |

## 5. 已取得的验证结果

### 5.1 Windows Qt 6 Debug

- 构建：通过；
- CTest：14/14 通过，当前源码总耗时 12.42 s；
- 完整应用离屏冒烟：通过；
- 16,384 点、请求 1,000 FPS：999.95 FPS，65.53 MB/s，显示采样 34.66 FPS；
- 65,536 点包络：约 0.423 ms/次；
- 65,536 点完整 Raster 控件：约 0.477 ms/帧；
- 1920×1080 主窗口：32.62 FPS，末次绘制约 0.639 ms，实际绘制完成延迟 P95 16.1 ms。

### 5.2 Windows Qt 6 Release

- Qt 6.10.2、MinGW 13.1、CMake/Ninja；
- `RTSA_ENABLE_WARNINGS_AS_ERRORS=ON`；
- 构建：通过，无编译警告；
- CTest：14/14 通过，总耗时 11.64 s；
- 采集异常注入、清理和重新启动：通过；
- 16,384 点、请求 1,000 FPS：实测 1,000.01 FPS；
- 对应完整频谱数据率：实测 65.54 MB/s；
- 16 ms 最新帧显示采样：实测 35.29 FPS；
- 65,536 点包络：约 0.0645 ms/次；
- 65,536 点完整 Raster 控件：约 0.4515 ms/帧；
- 1920×1080 主窗口：33.70 FPS，末次绘制约 0.694 ms；
- 实际绘制完成延迟 P95 为 16.5 ms，并纳入 `<150 ms` 自动门禁。

Release 自动测试包含生成率不低于 900 FPS、显示采样不低于 30 FPS 的回归门槛。本次最终构建和测试均在 2026-07-26 执行通过。

### 5.3 当前阶段验收结论

Windows Qt 6 上位机开发及模拟数据正常显示已经完成。Debug/Release 自动测试、安装目录场景启动和真实窗口可视化检查均通过；最终窗口中频谱曲线、固定信号、扫频信号、噪声底、坐标轴、控制面板及实时状态信息显示正常，无裁剪或异常重叠。Linux、板端和 DMA 验证不在本次验收范围内。

### 5.4 独立交付审计

完成实现后以事实、故障/并发、测试/API 兼容三个独立只读视角审查当前工作区。审查发现并已修复：JSON 保存和边界问题、场景读取前限长、日志大小与消息处理器生命周期、暂停/错误状态竞争、框选缩放复位、重复源序号的新会话显示、丢帧分类、生命周期/触摸信号测试，以及实际绘制完成延迟 P95。所有修复均已进入上述 14 项 Debug/Release 门禁；外部环境缺口仍按第 6 节保留为未验证。

## 6. 后续阶段项目

以下项目保留到后续 PetaLinux、板端和 DMA 联调阶段。它们不能用 Windows 模拟结果代替，但不影响当前“Windows 上位机 + 模拟频谱显示”阶段已经完成的结论：

1. Qt 5.15.x 实际构建：当前机器只发现 Qt 6.10.2 工具链；
2. ARM64 交叉编译：当前工作区没有 PetaLinux 2023.2 工程生成的 SDK/sysroot；
3. 板端 Raster QPA、CPU、内存和显示帧率：当前没有连接 ZU47DR 目标板；
4. 24 小时长稳：已提供可复现命令，但尚未实际运行完整 24 小时；
5. DMA 接入：PL 帧格式、AXI DMA 模式、驱动设备/API 和缓存一致性约束仍为 TBD；
6. 真实吞吐、丢帧、端到端延迟和幅度标定：必须在 PL/驱动完成后联调。

当前延后项为 COULD 级 OBW、-3/-6 dB 相对带宽和瀑布图。DMA 自动重试、错误分类和过载策略必须结合真实驱动 API 完成，不能用模拟异常替代。

## 7. 下一阶段输入清单

进入 PetaLinux/DMA 阶段前需提供：

- PetaLinux 2023.2 工程或生成的 extensible SDK；
- 目标 rootfs 中 Qt 版本及 Widgets、PNG、linuxfb 插件清单；
- 屏幕分辨率、像素格式、framebuffer/DRM 设备和触控输入信息；
- 每帧魔数、版本、头长、总长、序号、时间戳、中心频率、Span 和点数；
- 频点位宽、符号、定点比例、偏移和字节序；
- AXI DMA Simple/SG 模式、TLAST 语义和最大帧长；
- Linux 用户态打开、等待、映射、取帧、归还和复位 API；
- DMA coherent/cache 同步责任和缓冲所有权规则。

获得这些输入后，应新增 `DmaSpectrumSource` 和独立协议解析器，不修改频谱控件、轨迹处理和导出模块。
