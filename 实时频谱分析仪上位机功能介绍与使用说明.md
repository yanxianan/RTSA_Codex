# RFSoC ZU47DR 实时频谱分析仪上位机功能介绍与使用说明

## 1. 文档目的与当前范围

本文面向当前 Windows PC 版实时频谱分析仪上位机的使用者，介绍软件用途、界面组成、模拟信号配置、频谱操作、测量、文件导出、运行状态和常见故障处理。

当前版本已经完成 Qt 上位机和模拟频谱显示，使用软件模拟源代替尚未接通的 PL/AXI DMA 数据。频谱绘制、轨迹处理、Marker、测量、导出和状态监控均可在 PC 上正常使用。PetaLinux、ZU47DR 板端和真实 DMA 数据源属于后续联调阶段。

当前幅度默认采用 `dBFS`，表示相对于数字满量程的幅度。未完成射频链路标定前，界面显示的 dBFS 不能直接解释为准确的 dBm。

## 2. 软件总体功能

上位机提供以下功能：

- 实时显示频谱曲线、频率轴、幅度轴和网格；
- 支持 1,024～65,536 个频点；
- 支持 Center/Span 和 Start/Stop 两种频率设置方式；
- 支持鼠标、键盘和触摸方式缩放、平移频谱；
- 提供噪声、两个固定信号、扫频信号和随机瞬态信号；
- 支持实时刷新、平均、最大保持和最小保持四种轨迹模式；
- 支持 M1～M4 四个 Marker、峰值搜索、相邻峰搜索和 Delta 测量；
- 支持指定频率区间内的峰值和信道功率测量；
- 支持保存 PNG 频谱截图和完整频点 CSV 数据；
- 支持将模拟参数保存为可重复加载的 JSON 场景；
- 实时显示输入 FPS、显示 FPS、数据率、丢帧、延迟和处理耗时；
- 自动保存常用界面与测量设置；
- 完全使用 CPU Raster 绘制，不依赖 GPU 或 OpenGL。

## 3. 安装目录与启动

### 3.1 可直接运行的程序

完整发布目录为：

```text
D:\Study\Codex\RTSA\build\install-final
```

可直接双击的程序为：

```text
D:\Study\Codex\RTSA\build\install-final\bin\rtsa_app.exe
```

`bin` 目录同时包含 Qt DLL、MinGW 运行库和 `platforms/qwindows.dll`。移动或交付软件时必须复制整个 `install-final` 目录，不能只复制 `rtsa_app.exe`，也不能改变 `bin` 内插件子目录的相对位置。

### 3.2 普通启动

1. 打开 `build\install-final\bin` 文件夹。
2. 双击 `rtsa_app.exe`。
3. 程序打开后检查右侧“运行状态”为“已初始化”或“已停止”。
4. 点击“开始/继续”，频谱绘图区开始更新。

### 3.3 命令行启动

在 PowerShell 中可使用：

```powershell
cd D:\Study\Codex\RTSA\build\install-final

# 默认模拟数据源
.\bin\rtsa_app.exe --source simulated

# 全屏启动
.\bin\rtsa_app.exe --source simulated --fullscreen

# 加载默认模拟场景
.\bin\rtsa_app.exe --source simulated `
  --scenario .\share\rtsa\config\default-simulation.json

# 查看全部参数
.\bin\rtsa_app.exe --help
```

当前 `--source` 支持 `simulated` 和预留的 `dma` 标识。真实 DMA 尚未实现时选择 `dma` 会明确报错退出，不会静默切换到模拟数据。

## 4. 主界面组成

主窗口分为三个区域：

1. 左侧频谱绘图区：显示当前频谱、坐标、网格和 Marker。
2. 右侧滚动配置栏：包含数据源、模拟信号、显示、轨迹、测量、文件和运行状态设置。
3. 底部状态栏：快速显示运行状态、输入/显示 FPS、数据率、丢帧、延迟和绘制时间。

右侧内容较多，可使用鼠标滚轮或拖动滚动条浏览。深色高对比度主题会区分三种状态：

- 正常控件：深色输入背景和高亮文字，可修改；
- 禁用控件：较浅的深灰背景和清晰灰白文字，可读取但暂不可修改；
- 当前选中内容：蓝色高亮和白色文字。

## 5. 数据源与频率设置

### 5.1 Center/Span

- “中心频率”设置显示频段的中心，单位为 MHz；
- “频宽（Span）”设置完整显示带宽，单位为 MHz；
- 显示起点等于 `Center - Span/2`；
- 显示终点等于 `Center + Span/2`。

输入新数值后按 Enter，或单击其他控件使输入框失去焦点，设置才会提交。

### 5.2 Start/Stop

- “起始频率”设置频谱左边界；
- “终止频率”设置频谱右边界；
- 修改 Start/Stop 后，Center/Span 自动同步；
- Stop 必须大于 Start，非法范围不会被接受。

Center/Span 与 Start/Stop 是同一频率范围的两种表达方式，不是两组独立参数。

### 5.3 频点数

可选频点数：

| 频点数 | 典型用途 |
|---:|---|
| 1,024～4,096 | 快速功能操作和低负载测试 |
| 8,192～16,384 | 常规实时频谱观察 |
| 32,768～65,536 | 高分辨率和绘制性能测试 |

频点数越大，频率分辨率越高，同时每帧数据量和 CPU/内存带宽需求也越高。切换频点数后，不兼容的平均或保持轨迹会自动清空。

### 5.4 输入帧率和无等待压测

- 模拟输入帧率范围为 1～1,000 帧/s；
- GUI 与输入线程解耦，只显示刷新时刻最新的完整帧；
- 输入帧率高于显示帧率时不会建立无限队列；
- 启用“无等待压测”后，模拟线程尽可能快地生成数据；
- 无等待模式下输入帧率框会被禁用，但数值仍以高对比度方式清晰显示；
- 无等待模式会明显提高 CPU 占用，只用于压力测试。

### 5.5 采集按钮

| 按钮 | 功能 |
|---|---|
| 开始/继续 | 从停止状态开始连续采集，或从暂停状态恢复 |
| 暂停 | 暂停产生新数据，保留最后一帧、轨迹和 Marker |
| 停止 | 停止并回收采集线程 |
| 单次 | 只生成并显示一帧，然后停止 |

按钮会根据数据源状态自动启用或禁用。禁用表示当前状态下该操作无效，不是程序故障。

## 6. 模拟信号设置

### 6.1 噪声

- “噪声底”设置平均噪声幅度，单位 dBFS；
- “噪声起伏”设置每个频点的随机波动强度，单位 dB；
- 相同随机种子和相同配置可产生可重复的数据序列。

### 6.2 固定信号 1 和固定信号 2

每个固定信号都可以独立设置：

- 启用/关闭；
- 相对中心频率的偏移，单位 MHz；
- 信号幅度，单位 dBFS；
- 等效带宽，单位 bins。

信号实际频率为：

```text
信号频率 = Center + 频率偏移
```

增大带宽会使信号在更多频点上展开。信号幅度和噪声在内部按线性功率合成，不是简单相加 dB 数值。

### 6.3 扫频信号

扫频功能支持：

- 启用或关闭；
- 向上、向下和往返三种方向；
- 相对 Center 的起点偏移和终点偏移；
- 0.1～3,600 秒扫频周期；
- 独立扫频幅度。

扫频起点必须小于终点。往返模式到达终点后反向移动，不会瞬间跳回起点。

### 6.4 瞬态信号

- “瞬态概率”表示每一帧触发瞬态的百分比；
- “瞬态幅度”设置瞬态信号强度；
- “瞬态持续”设置一次触发后保持的时间；
- 一次瞬态在持续期间保持同一频率，便于观察短时事件。

将瞬态概率设为 `0` 可关闭瞬态信号。

### 6.5 建议的功能检查场景

第一次运行建议使用默认值：

- Center：1,000 MHz；
- Span：200 MHz；
- 频点数：16,384；
- 输入帧率：200 帧/s；
- 噪声底：-110 dBFS；
- 单音 1：-20 MHz、-35 dBFS；
- 单音 2：+35 MHz、-18 dBFS；
- 扫频：-70～+70 MHz、4 秒周期；
- 瞬态概率：0.2 %/帧。

点击“开始/继续”后，应看到噪声底、两个固定峰和一个随时间移动的扫频峰。

## 7. 频谱绘图区操作

### 7.1 鼠标操作

| 操作 | 结果 |
|---|---|
| 单击频谱 | 在单击频率设置当前活动 Marker |
| 滚轮向上 | 以鼠标位置为中心缩小 Span，查看细节 |
| 滚轮向下 | 扩大 Span |
| 左键拖动 | 水平平移频率范围 |
| Shift + 左键拖动 | 框选一个频率区间并放大 |
| 双击频谱 | 恢复用户设定的完整频率范围 |

### 7.2 键盘操作

先单击频谱绘图区使其获得键盘焦点：

| 按键 | 功能 |
|---|---|
| `Home` 或 `R` | 恢复完整频率范围 |
| `+` 或 `=` | 以中心频率放大 |
| `-` | 以中心频率缩小 |
| `←` | 向低频方向平移当前 Span 的 10% |
| `→` | 向高频方向平移当前 Span 的 10% |
| `Esc` | 退出全屏 |

### 7.3 触摸操作

- Pinch：双指缩放 Span；
- Pan：双指或系统识别的平移手势移动中心频率。

触摸功能已经完成 Qt 事件测试，实际触摸屏手感仍需在目标硬件上确认。

## 8. 幅度显示与外观

- “参考电平”控制纵轴顶部幅度；
- “底部电平”控制纵轴底部幅度，必须小于参考电平；
- “垂直刻度”控制每格幅度范围；
- “自动量程”根据当前有效频谱设置合适的上下边界；
- 曲线颜色可选翠绿、青蓝、明黄或白色；
- 曲线线宽可选 1～4 px；
- 网格可以显示或隐藏；
- 绘图区可切换深色或浅色主题；
- “进入全屏”扩大工作区域，再次点击或按 Esc 退出。

主应用控制栏固定使用高对比度深色主题；“绘图区主题”只改变频谱画布，不会把右侧文字重新变成低对比度颜色。

## 9. 轨迹处理

| 模式 | 说明 |
|---|---|
| 实时刷新（Clear/Write） | 每次使用最新频谱覆盖旧频谱 |
| 平均（Average） | 对最近 N 帧进行平滑，降低随机噪声波动 |
| 最大保持（Max Hold） | 保存每个频点出现过的最大值，适合捕获扫频和瞬态 |
| 最小保持（Min Hold） | 保存每个频点出现过的最小值 |

选择“平均”后“平均次数”输入框自动启用，范围为 1～1,024。其他模式下该输入框禁用，但仍可清楚读取上次设置值。

点击“清除历史轨迹”可立即重置 Average、Max Hold 或 Min Hold 的历史。修改频点数、Center、Span 或幅度单位时，不兼容的历史也会自动重置。

## 10. Marker 与峰值搜索

### 10.1 四个独立 Marker

1. 在“活动标记”中选择 M1、M2、M3 或 M4。
2. 单击频谱曲线附近设置该 Marker。
3. “当前读数”显示 Marker 频率和幅度。
4. 切换活动 Marker 后可继续设置其他 Marker。

Marker 按频率保存位置。新帧到达时幅度会自动更新；频率移出当前 Span 后 Marker 显示为无效，而不是错误地吸附到边界。

### 10.2 峰值搜索

- “峰值门限”用于过滤低于门限的局部峰；
- “搜索峰值”将活动 Marker 放到当前完整频谱的最大峰；
- “上一个峰值”和“下一个峰值”在满足门限的局部峰之间移动；
- 搜索使用完整频点数据，而不是屏幕压缩后的绘制点。

若搜索不到峰值，先检查门限是否设得过高，并确认已经获得有效频谱帧。

### 10.3 Delta 测量

- 启用“相对 M1”后，M1 作为参考；
- 活动 Marker 为 M2～M4 时显示相对 M1 的频率差和幅度差；
- M1 或活动 Marker 无效时，Delta 结果也无效。

“清除当前”只删除活动 Marker，“清除全部”删除 M1～M4。

## 11. 区间测量

### 11.1 区间峰值

1. 设置区间起始频率和终止频率；
2. 确保起点小于终点，并且区间与当前 Span 有交集；
3. 点击“区间峰值”；
4. 结果区显示区间内最大频点的频率和幅度。

### 11.2 信道功率

1. 设置要积分的频率区间；
2. 点击“信道功率”；
3. 软件对区间内所有完整频点进行功率积分；
4. dB 数据先转换为线性功率，求和后再转换回 dB。

模拟源结果会显示 dBFS 和“未校准”。在没有 RBW/标定协议时，该结果用于软件功能验证，不应代替校准仪器的绝对功率结果。

## 12. 文件功能

### 12.1 保存 PNG 截图

点击“保存 PNG 截图”，选择目标文件。PNG 只截取频谱绘图区，包含坐标、网格、轨迹和 Marker，不包含右侧配置面板。

### 12.2 导出当前 CSV

获得至少一帧有效数据后“导出当前 CSV”可用。CSV 包含：

- 帧序号和时间戳；
- Center、Span 和频点数；
- 幅度单位及校准状态；
- 每个频点的索引、频率和幅度。

导出使用点击按钮时的不可变完整帧快照，在后台写入，不会因新帧到达而改变。写入采用原子提交，失败不会留下看似完整的半文件。

### 12.3 保存模拟场景

点击“保存模拟场景”可把当前模拟参数保存为 JSON。场景包括频率、点数、帧率、噪声、固定信号、扫频和瞬态参数，保存前执行与加载相同的完整校验。

安装目录提供三个场景：

| 文件 | 用途 |
|---|---|
| `default-simulation.json` | 默认功能演示 |
| `unthrottled-stress.json` | 16,384 点无等待性能压力测试 |
| `fault-injection.json` | 暂停、序号跳变、异常帧和突发故障测试 |

它们位于：

```text
build\install-final\share\rtsa\config
```

## 13. 运行状态解释

| 指标 | 含义 |
|---|---|
| 状态 | 数据源当前生命周期状态 |
| 输入速率 | 数据源产生频谱帧的速率 |
| 显示速率 | GUI 实际显示不同频谱帧的速率 |
| 输入数据率 | 完整频谱数据吞吐率 |
| 源/传输丢帧 | 数据源未形成或未交付的帧；未来用于 DMA 传输丢帧 |
| 无效帧 | 因点数、数值或元数据非法而被拒绝的帧 |
| 处理丢帧 | 轨迹处理阶段未发布的帧 |
| 发布/提交 | 流水线发布帧数与源提交帧数 |
| 显示跳过/已显示 | GUI 为保持实时性主动跳过的旧帧数与已显示帧数 |
| 帧龄 | 当前可见帧从产生到显示的时间 |
| 显示延迟 P95 | 最近 256 个实际绘制完成样本的 95 分位延迟 |
| 处理耗时 | 单帧校验和轨迹处理时间 |
| 绘制耗时 | 最近一次 CPU Raster 绘制时间 |
| 处理队列深度 | 最新帧架构下同步等待队列深度，正常为 0 |
| 最近错误 | 最近一次数据源或处理错误 |

输入 FPS 高于显示 FPS 是正常现象。软件设计目标是显示最新完整频谱，而不是把每个输入帧都排队绘制；否则高输入速率会造成持续增长的显示延迟。

## 14. 推荐操作流程

### 14.1 快速确认软件正常

1. 双击完整安装目录中的 `rtsa_app.exe`。
2. 保持默认 16,384 点和 200 帧/s。
3. 点击“开始/继续”。
4. 检查噪声底、两个固定峰和移动扫频峰是否出现。
5. 确认底部状态栏的输入 FPS、显示 FPS 和数据率持续更新。
6. 点击“暂停”，确认频谱保持不变。
7. 点击“开始/继续”，确认恢复更新。
8. 选择 M1 并点击“搜索峰值”，确认读数出现。
9. 分别尝试 Average 和 Max Hold。
10. 保存一张 PNG，并导出一份 CSV。

### 14.2 观察弱信号

1. 适当减小 Span；
2. 使用 Average 降低噪声波动；
3. 增大平均次数；
4. 使用自动量程或手工调整参考/底部电平；
5. 设置合适的峰值门限后执行峰值搜索。

### 14.3 捕获扫频或瞬态

1. 选择 Max Hold；
2. 清除历史轨迹；
3. 启用扫频或提高瞬态概率；
4. 运行足够时间使事件覆盖目标频段；
5. 暂停后使用 Marker 或区间峰值进行分析。

## 15. 配置保存

程序正常退出时保存窗口、频率范围、频点数、模拟参数、幅度范围、轨迹设置和绘图区外观。下次启动会自动恢复。

若保存的配置损坏、越界或来自未知未来版本，软件会整体回退到安全默认值，不会使用半有效参数继续运行。命令行加载的模拟场景优先于已保存的模拟设置。

## 16. 日志与故障排查

默认日志位于 Qt 的 Windows 应用本地数据目录下，使用有界轮转，避免长期占满磁盘。需要指定日志目录时可使用：

```powershell
.\bin\rtsa_app.exe `
  --log-dir D:\RTSA-Logs `
  --log-level info `
  --log-max-bytes 5242880 `
  --log-retained-files 3
```

常见问题：

| 现象 | 处理方法 |
|---|---|
| 提示缺少 Qt6Core.dll、Qt6Widgets.dll 等 | 使用 `build\install-final` 完整目录，不要运行或复制单独 EXE |
| 提示找不到 Qt platform plugin | 检查 `bin\platforms\qwindows.dll` 是否存在并保持目录结构 |
| 右侧输入框文字不清楚 | 确认使用最新安装版；新版使用 Fusion 高对比度主题 |
| 点击开始后没有频谱 | 检查数据源状态、最近错误，并先恢复默认模拟场景 |
| CSV 按钮不可用 | 先开始采集或执行一次单次采集，确保已有有效帧 |
| 峰值搜索无结果 | 降低峰值门限，并确认目标信号位于当前 Span 内 |
| 输入 FPS 很高但显示 FPS 较低 | 正常的最新帧显示策略，不代表数据源故障 |
| 无等待压测时 CPU 很高 | 正常现象；关闭无等待模式恢复限速 |
| `WrapVulkanHeaders` 缺失 | Qt 可选探测提示；软件不使用 Vulkan/OpenGL |
| 选择 DMA 后退出 | 当前阶段的预期保护行为，等待真实 DMA 接口接入 |

## 17. 当前阶段边界

本阶段已经完成并验证：

- Windows Qt 6 上位机；
- 模拟数据源；
- CPU Raster 实时频谱显示；
- 轨迹、Marker、区间测量、PNG、CSV 和配置保存；
- 完整 Windows 运行库部署；
- 自动化功能、性能和 GUI 测试。

后续阶段包括：

- PetaLinux 2023.2/Qt 5 交叉编译；
- ZU47DR 板端显示和触摸实测；
- AXI DMA 用户态数据源；
- PL/PS 帧协议解析；
- DMA 吞吐、丢帧、延迟和缓存一致性验证；
- 射频幅度标定；
- 瀑布图等扩展功能。

这些后续项目不影响当前 Windows 上位机和模拟频谱显示的使用。
