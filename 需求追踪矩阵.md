# RFSoC ZU47DR 实时频谱分析仪需求追踪矩阵

## 1. 状态定义

本矩阵以《实时频谱分析仪软件需求规格说明书》为基线，以当前工作区源码、自动测试和 2026-07-26 实测结果为证据。

当前阶段只验收 Windows Qt 上位机和模拟数据频谱显示，该范围已经完成。PetaLinux/Qt 5、ZU47DR 板端、AXI DMA 及板端性能条目继续保留用于后续追踪，其“外部阻塞”状态只描述后续阶段所需输入，不阻塞当前阶段验收。

| 状态 | 含义 |
|---|---|
| 已验证 | 当前 Windows Qt 6 PC 范围内已有实现和自动化证据 |
| 已实现，板端待验 | 代码路径已实现，但触摸、QPA、性能等仍需 ZU47DR 实机确认 |
| 外部阻塞 | 后续阶段需要 PetaLinux SDK、目标板、PL 协议或 DMA API；不阻塞当前阶段 |
| 延后 | COULD/后续范围，当前未实现 |
| 未达目标 | TARGET 不是一期 MUST，但当前测量未达到目标值 |

## 2. 功能需求

### 2.1 应用与模拟源

| 编号 | 状态 | 当前证据 |
|---|---|---|
| FR-APP-001 | 已验证 | `rtsa_app_smoke`、场景 smoke 和 Windows Qt 6 Release 构建通过 |
| FR-APP-002 | 外部阻塞 | 缺少 PetaLinux 2023.2 SDK/sysroot 和 ZU47DR 目标板 |
| FR-APP-003 | 已验证 | `ConfigurationStore` 安全默认值、损坏回退和 GUI smoke |
| FR-APP-004 | 已验证 | `--source simulated/dma`、工厂解析及精确失败退出码测试 |
| FR-APP-005 | 已验证 | 数据源析构/窗口退出 join；低速等待、故障暂停和 smoke 正常退出测试 |
| FR-APP-006 | 已验证 | `--fullscreen`、`--source`、`--scenario`、日志及 smoke 命令行选项 |
| FR-SIM-001 | 已验证 | 噪声底与波动配置、模拟源测试 |
| FR-SIM-002 | 已验证 | 两固定信号、频率/幅度/带宽配置和行为测试 |
| FR-SIM-003 | 已验证 | 扫频起止、周期、Up/Down/PingPong 和完整往返测试 |
| FR-SIM-004 | 已验证 | 瞬态概率、幅度、持续时间和固定频率持续测试 |
| FR-SIM-005 | 已验证 | 1,024～65,536 点控件、场景校验和多点数测试 |
| FR-SIM-006 | 已验证 | 1～1,000 FPS 和无等待模式；1000 FPS 性能门禁 |
| FR-SIM-007 | 已验证 | 固定种子逐频点一致性测试 |
| FR-SIM-008 | 已验证 | 场景 v2 支持数据暂停、序号跳变、NaN 异常帧和突发帧；行为测试及故障场景应用 smoke |
| FR-SIM-009 | 已验证 | `addDecibels` 在线性功率域合成；等功率单音约 +3.01 dB 测试 |
| FR-SIM-010 | 已验证 | JSON v1 迁移、v2 严格加载、`QSaveFile` 原子保存和往返测试 |

### 2.2 数据源与频谱显示

| 编号 | 状态 | 当前证据 |
|---|---|---|
| FR-SRC-001 | 已验证 | Initialized/Starting/Running/Paused/Stopped/Error 生命周期测试 |
| FR-SRC-002 | 已验证 | 开始、暂停、继续、停止、单次及 50 次暂停恢复测试 |
| FR-SRC-003 | 已验证 | `stateChanged`、`errorOccurred` Qt 轻量信号 |
| FR-SRC-004 | 已验证 | `ISpectrumSource` 注入测试；模拟源和外部源共用同一 `SpectrumPlotWidget` |
| FR-SRC-005 | 已验证 | 配置 epoch、帧池 resize 和轨迹不兼容历史重置测试 |
| FR-SRC-006 | 外部阻塞 | 自动重试策略应结合 DMA 可恢复错误分类实现；当前模拟源允许明确停止后重启 |
| FR-DSP-001 | 已验证 | Raster 曲线、轴、网格、单位离屏渲染测试 |
| FR-DSP-002 | 已验证 | Center/Span 与 Start/Stop 双向同步 GUI 测试 |
| FR-DSP-003 | 已验证 | 参考、底部、dB/格双向同步和自动量程 |
| FR-DSP-004 | 已验证 | Hz/kHz/MHz/GHz 及 dBFS/dBm/dBc/Linear 单位格式 |
| FR-DSP-005 | 已验证 | QWidget/QPainter CPU Raster；无 OpenGL/Qt Quick/Vulkan 链接 |
| FR-DSP-006 | 已验证 | 每像素列 min/max `EnvelopeReducer` |
| FR-DSP-007 | 已验证 | 包络列数受画布宽度约束；绘制点数 O(width) |
| FR-DSP-008 | 已验证 | 轴、网格和文字静态 QImage 只在尺寸/轴/主题变化时失效 |
| FR-DSP-009 | 已验证 | resize、低点数全宽、1920x1080 离屏 GUI 测试 |
| FR-DSP-010 | 已验证 | 暂停保留最后帧和 Marker；暂停恢复测试 |
| FR-DSP-011 | 已验证 | 滚轮及框选缩放后，双击/Home/R 和按钮路径恢复完整频率范围 |
| FR-DSP-012 | 已验证 | 滚轮缩放、拖动平移、Shift 框选缩放测试 |
| FR-DSP-013 | 已实现，板端待验 | Qt Pinch/Pan 手势信号参数测试通过；目标触控硬件待验 |
| FR-DSP-014 | 已验证 | 颜色预设、1～4 px 线宽、网格开关、深浅绘图区主题和 schema v4 持久化 |

### 2.3 轨迹、Marker 与测量

| 编号 | 状态 | 当前证据 |
|---|---|---|
| FR-TRACE-001 | 已验证 | Clear/Write 测试和 UI 模式 |
| FR-TRACE-002 | 已验证 | Max Hold 与清除历史测试 |
| FR-TRACE-003 | 已验证 | Average-N 配置和收敛测试 |
| FR-TRACE-004 | 已验证 | Min Hold 模式及复用测试 |
| FR-TRACE-005 | 已验证 | 点数、epoch、Center、Span、单位变化自动重置 |
| FR-TRACE-006 | 已验证 | 输入为只读共享帧，非 Clear/Write 使用独立有界输出池 |
| FR-MKR-001 | 已验证 | M1～M4 独立频率/幅度读数 |
| FR-MKR-002 | 已验证 | Peak Search 使用完整频点测试 |
| FR-MKR-003 | 已验证 | 四 Marker 状态、渲染和清除当前/全部测试 |
| FR-MKR-004 | 已验证 | 活动 M2～M4 相对 M1 的频差/幅差测试 |
| FR-MKR-005 | 已验证 | Next/Previous Peak 环回搜索和峰值门限测试 |
| FR-MKR-006 | 已验证 | Marker/Peak 直接读取完整 `frame->bins` |
| FR-MKR-007 | 已验证 | 按频率保存，换帧/缩放后重新映射；超出 Span 自动清除 |
| FR-MEAS-001 | 已验证 | `SpectrumMeasurements::peakInRange` 与 GUI 区间峰值 |
| FR-MEAS-002 | 已验证 | dB 先转线性积分、Linear Power 直接求和；GUI 信道功率 |
| FR-MEAS-003 | 延后 | OBW 为 COULD，当前未实现 |
| FR-MEAS-004 | 延后 | -3/-6 dB 带宽为 COULD，当前未实现 |
| FR-MEAS-005 | 已验证 | Marker、区间峰值和信道功率均显示单位、有效性和校准状态 |

### 2.4 状态、文件与配置

| 编号 | 状态 | 当前证据 |
|---|---|---|
| FR-STAT-001 | 已验证 | Initialized 等状态与运行时间标签 |
| FR-STAT-002 | 已验证 | 输入 FPS、显示 FPS、FFT 点数标签 |
| FR-STAT-003 | 已验证 | 平均数据率、提交/发布帧；源、无效、处理和基于发布序号的显示丢帧分类互不混淆 |
| FR-STAT-004 | 已验证 | 最近错误及本地时间戳 |
| FR-STAT-005 | 已验证 | 处理耗时、绘制耗时、同步队列深度 0、帧龄和 `paintEvent` 完成后采样的 256 样本显示延迟 P95 |
| FR-STAT-006 | 已验证 | 原子计数；采集热路径无全局高频锁 |
| FR-IO-001 | 已验证 | Raster 绘图区 PNG 截图 |
| FR-IO-002 | 已验证 | CSV 使用不可变完整帧快照，不使用包络 |
| FR-IO-003 | 已验证 | CSV 元数据、bin、frequency、动态 amplitude 单位列测试 |
| FR-IO-004 | 已验证 | 窗口、源、模拟、幅度、轨迹和外观 schema v4 保存恢复 |
| FR-IO-005 | 已验证 | QSettings 强制 atomic sync；场景和 CSV 使用 `QSaveFile` |
| FR-IO-006 | 已验证 | 未来版本整体回退并由 Qt 日志桥记录 warning |
| FR-IO-007 | 已验证 | QtConcurrent 后台 CSV；按钮禁用和完成/失败提示 |

## 3. 性能需求

| 编号 | 状态 | 当前证据 |
|---|---|---|
| NFR-PERF-001 | 已验证 | 1,024～65,536 点数据结构、场景、绘图和测试 |
| NFR-PERF-002 | 已验证（PC） | Release 1920x1080 实测 33.70 FPS；板端待验 |
| NFR-PERF-003 | 未达目标 | 当前 Release 实测约 34 FPS，未达到 60 FPS TARGET；不阻塞 30 FPS MUST |
| NFR-PERF-004 | 已验证 | 1000 FPS 输入与约 35.29 FPS 最新帧显示采样解耦 |
| NFR-PERF-005 | 已验证 | 16,384 点、1000.01 FPS、65.54 MB/s，GUI 35.29 FPS |
| NFR-PERF-006 | 外部阻塞 | 目标板 16,384 点/1000 FPS 需 SDK、DMA 和板卡 |
| NFR-PERF-007 | 外部阻塞 | 目标板 65,536 点/200 FPS 需板卡实测 |
| NFR-PERF-008 | 外部阻塞 | 262.14 MB/s 极限压测取决于 DMA/DDR/CPU/协议 |
| NFR-PERF-009 | 已验证（PC） | 连续显示时在实际绘制完成后采样固定 256 样本 P95，实测 16.5 ms；GUI 门禁 <150 ms；板端待验 |
| NFR-PERF-010 | 已验证 | 单遍包络 O(N)，输出 O(width)；65,536 点约 0.0645 ms |
| NFR-PERF-011 | 已验证 | GUI 只原子读取最新共享帧，不等待源或持有源互斥锁 |
| NFR-PERF-012 | 外部阻塞 | 代码为 CPU Raster，但目标板 QPA/无 OpenGL 实测尚未执行 |

## 4. 阶段验收条款

本次验收覆盖 Windows Qt 模拟版的构建、功能、模拟数据和显示性能。下表继续展示完整项目条款以便后续追踪；Qt 5、24 小时长稳、DMA 实机和 ARM64 板端条目不属于本次验收范围。

| 条款 | 状态 | 说明 |
|---|---|---|
| 1 Windows Qt 模拟版 | 已验证 | Release 应用和三套场景可运行 |
| 2 Qt 6.x 与 Qt 5.15.x | 部分完成 | Qt 6.10.2 已验证；Qt 5.15 SDK 尚未提供 |
| 3 模拟噪声/多信号/扫频/瞬态 | 已验证 | 行为及场景测试 |
| 4 1,024～65,536 点 | 已验证 | 数据、UI 和性能测试 |
| 5 1920x1080、16K、1000 FPS、GUI >=30 FPS | 已验证（PC） | 1000.01 输入 FPS、35.29 显示采样 FPS |
| 6 无 GPU、窄峰保留 | 已验证（PC） | Raster 与 65K 单点峰包络测试 |
| 7 频谱/坐标/Marker/Peak/Average/Max Hold | 已验证 | 自动测试覆盖 |
| 8 开始/暂停/继续/停止/缩放 | 已验证 | 自动测试覆盖 |
| 9 状态/FPS/数据率/丢帧 | 已验证 | GUI 与统计测试 |
| 10 PNG/CSV/配置 | 已验证 | 导出和 schema v4 测试 |
| 11 连续 24 小时 | 未验证 | 当前仅短时、20 轮重复和压力门禁，不能替代 24 小时 |
| 12 DMA 源无需重写显示 | 已验证（接口） | `ISpectrumSource` 注入边界和外部源 GUI 测试 |
| 13 ARM64 说明和目标板模拟运行 | 部分完成 | 说明已完成；交叉编译和板端运行外部阻塞 |

当前阶段结论：Windows Qt 上位机及模拟频谱显示验收通过。Linux/PetaLinux、Qt 5、ZU47DR 板端和真实 DMA 通道待用户完成相应环境与接口后进入下一阶段验证。

## 5. 后续阶段输入与风险

1. 提供 PetaLinux 2023.2 extensible SDK/sysroot，完成 Qt 5.15 ARM64 构建。
2. 提供 ZU47DR 目标板、显示/触摸设备和 QPA 插件，完成 linuxfb/xcb 实测。
3. 签署 PL/PS 帧格式、AXI DMA 模式、缓存一致性和用户态驱动 API。
4. 执行 PC 与目标板各 24 小时长稳，记录内存、CPU、温度、句柄/文件描述符和日志增长。
5. 目标板完成 30 FPS、P95 延迟、16K/1000 FPS 和 65K/200 FPS 性能验收。
6. `FR-SRC-006` 自动重试、DMA 错误分类和过载策略需在真实 DMA 适配器中完成。
7. OBW、相对带宽和瀑布图属于后续 COULD/扩展范围。
