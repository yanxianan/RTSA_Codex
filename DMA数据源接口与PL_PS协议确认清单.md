# DMA 数据源接口与 PL/PS 协议确认清单

## 1. 文档用途

本文是 PL、Linux 驱动和 PS 应用三方联调的输入与签署清单。所有 `待确认` 项必须在实现真实 `DmaSpectrumSource` 前由责任方给出书面结论。本文中的帧头仅为建议草案，不代表未经确认即可固化到 PL。

## 2. 软件接入边界

真实 DMA 数据源必须实现 `ISpectrumSource`，并向既有 `SpectrumPipeline` 发布只读 `SpectrumFrame`。频谱绘图、轨迹、Marker、CSV 和主窗口不得感知 DMA 缓冲布局。

```text
Linux DMA API -> IDmaDevice -> SpectrumFrameParser -> DmaSpectrumSource
                                                   -> ISpectrumSource::FrameSink
                                                   -> SpectrumPipeline
```

建议将设备访问和协议解析分开：

- `IDmaDevice`：打开、启动、等待、取得缓冲、归还、停止和复位；
- `SpectrumFrameParser`：只接收字节范围和协议描述，校验后生成统一元数据与频点；
- `DmaSpectrumSource`：工作线程、状态机、重试、统计和缓冲所有权编排。

## 3. `ISpectrumSource` 行为契约

| 项目 | 要求 |
|---|---|
| 状态 | 创建且具备安全默认配置后为 `Initialized`；采集生命周期使用 `Starting`、`Running`、`Paused`、`Stopped`、`Error` |
| 连续采集 | `start(Continuous)` 持续发布完整帧 |
| 单次采集 | `start(SingleFrame)` 只发布一个有效帧后停止 |
| 停止 | 必须唤醒阻塞等待并在有限时间内回收线程 |
| 帧回调 | `FrameSink` 返回 `false` 时累计处理/提交丢帧，不得无限重试同一帧 |
| 生命周期 | 回调持有的 `SpectrumFramePtr` 在所有消费者释放前保持只读有效 |
| 异常 | 可恢复错误分类统计；不可恢复错误转入 `Error`，保留最后有效频谱 |
| GUI 隔离 | 数据源不得直接访问 QWidget 或在采集线程绘图 |

## 4. 必须确认的 PL 输出参数

| 编号 | 参数 | 待填写结论 | 责任方 |
|---|---|---|---|
| PL-01 | DDC 输出采样率及可选范围 | 待确认 | PL |
| PL-02 | FFT 点数及运行时可选范围 | 待确认 | PL |
| PL-03 | FFT 窗函数及幅度修正 | 待确认 | PL/DSP |
| PL-04 | FFT Shift 是否完成、DC 所在索引 | 待确认 | PL |
| PL-05 | 输出为幅度、功率、功率平方、对数或复数 | 待确认 | PL/DSP |
| PL-06 | 每频点总位宽、小数位、符号和饱和规则 | 待确认 | PL |
| PL-07 | 字节序和 AXI Stream 字节排列 | 待确认 | PL |
| PL-08 | AXI Stream 数据宽度 | 待确认 | PL |
| PL-09 | `TLAST` 表示帧尾还是分包尾 | 待确认 | PL |
| PL-10 | 帧序号、时间戳、Center/Span 来源 | 待确认 | PL/PS |
| PL-11 | 峰值/持续帧率及允许过载行为 | 待确认 | PL/系统 |
| PL-12 | 帧间最小间隔及启动后首帧延迟 | 待确认 | PL |

## 5. 必须确认的 DMA 与驱动参数

| 编号 | 参数 | 待填写结论 | 责任方 |
|---|---|---|---|
| DMA-01 | AXI DMA Simple 或 Scatter-Gather | 待确认 | 驱动/PL |
| DMA-02 | 设备节点或 UIO/VFIO/字符设备接口 | 待确认 | 驱动 |
| DMA-03 | 最大单帧字节数及对齐 | 待确认 | 驱动/PL |
| DMA-04 | 环形缓冲数量和每缓冲容量 | 待确认 | 驱动 |
| DMA-05 | 阻塞等待、poll/epoll 或事件 API | 待确认 | 驱动 |
| DMA-06 | 超时、取消等待和关闭语义 | 待确认 | 驱动 |
| DMA-07 | 缓冲映射、取得和归还 API | 待确认 | 驱动 |
| DMA-08 | coherent 属性及 cache sync 责任 | 待确认 | 驱动 |
| DMA-09 | 溢出、DMA 错误和复位 API | 待确认 | 驱动 |
| DMA-10 | 多进程互斥和设备权限 | 待确认 | 驱动/系统 |
| DMA-11 | 设备树节点、时钟和中断依赖 | 待确认 | 驱动/PL |
| DMA-12 | 驱动统计计数器及读取方式 | 待确认 | 驱动 |

## 6. 建议的版本化帧头草案

如元数据与频谱载荷一起通过 DMA，建议使用固定字节序、显式长度和版本号。字段偏移、类型和对齐必须由三方确认后冻结。

| 字段 | 建议类型 | 说明 |
|---|---:|---|
| `magic` | `uint32` | 固定魔数，用于快速拒绝错位数据 |
| `protocolVersion` | `uint16` | 协议版本 |
| `headerBytes` | `uint16` | 帧头长度，允许向后兼容扩展 |
| `totalBytes` | `uint32` | 帧头与载荷总长度 |
| `flags` | `uint32` | Shift、校准、溢出、触发等标志 |
| `sequence` | `uint64` | 单调递增帧序号，回绕规则需定义 |
| `timestampNs` | `uint64` | 时间基准和精度需定义 |
| `centerFrequencyHz` | `uint64` | 中心频率 |
| `spanHz` | `uint64` | 显示频宽 |
| `binCount` | `uint32` | 频点数 |
| `sampleFormat` | `uint16` | 频点格式枚举 |
| `amplitudeUnit` | `uint16` | dBFS/dBm/线性等枚举 |
| `payloadBytes` | `uint32` | 实际载荷长度 |
| `headerCrc`/`payloadCrc` | 可选 | 是否使用及多项式需确认 |

若元数据通过寄存器控制面提供，必须定义“读取元数据”和“对应 DMA 帧”之间的一致性机制，避免配置更新时把新元数据配到旧载荷。

## 7. 解析器强制校验

解析任何字段前必须先验证实际缓冲长度，不信任帧头中的长度或点数。

1. 使用安全加法/乘法检查 `headerBytes + payloadBytes` 和 `binCount × bytesPerBin` 溢出；
2. `headerBytes` 不小于当前版本最小头长且不大于实际缓冲；
3. `totalBytes`、`payloadBytes` 与实际 DMA 有效字节一致；
4. `binCount` 位于协商范围且与 payload 匹配；
5. 枚举、flags 保留位和协议版本合法；
6. Center、Span 和换算后的幅度为有限有效值；
7. 帧序号跳变、重复和回退分类统计；
8. NaN、Inf、定点溢出和非法标定值拒绝；
9. 任何失败均不得越界读取、发布半帧或复用仍被消费者持有的缓冲。

未知未来协议版本默认拒绝；若要兼容，必须由独立版本解析器显式处理。

## 8. 缓冲所有权状态机

```text
Free -> DMA owned -> Completed -> User read-only -> Returned -> Free
```

- PL/驱动写入期间用户态不得读取；
- 用户态取得 Completed 缓冲后，驱动不得覆盖；
- 非一致性内存必须在所有权切换处由责任方执行 cache sync；
- 发布到异步消费者前应完成格式转换或通过具有明确归还回调的零拷贝对象持有缓冲；
- Stop/Reset 必须定义仍在用户态持有的缓冲如何回收；
- 不允许因 GUI 变慢而耗尽 DMA 环形缓冲，显示链只保留最新帧。

## 9. 错误分类和统计

至少提供以下独立计数，不能合并为一个“丢帧”：

- DMA 完成帧；
- DMA/驱动溢出；
- 等待超时；
- 帧头/版本错误；
- 长度或格式错误；
- 序号缺口与重复；
- 非有限/超范围频点；
- 处理提交丢弃；
- 已发布帧；
- GUI 跳过显示帧。

错误日志应限速，同类高频错误使用累计计数和周期摘要，避免占满系统分区。

## 10. 联调顺序与验收

1. 使用固定已知频谱帧验证帧头和字节序；
2. 验证 1,024、16,384 和最大点数的边界；
3. 注入错误 magic、版本、长度、点数、TLAST 和序号；
4. 验证 Stop 可取消阻塞等待并在限定时间内退出；
5. 验证连续帧下无半帧、撕裂和缓冲覆盖；
6. 验证典型及峰值吞吐、CPU、内存和丢帧；
7. 验证配置切换时帧与元数据的 epoch 一致性；
8. 验证断流、恢复、DMA reset 和应用重启；
9. 加载正式标定数据并与标准信号源比对；
10. 在目标 QPA 下执行 24 小时长稳。

建议验收记录包含每项的固件版本、驱动版本、应用版本、参数、期望、实测、日志路径和签署人。

## 11. 冻结前签署

| 角色 | 姓名/版本 | 日期 | 结论 |
|---|---|---|---|
| PL 负责人 | 待填写 | 待填写 | 待确认 |
| Linux 驱动负责人 | 待填写 | 待填写 | 待确认 |
| PS 应用负责人 | 待填写 | 待填写 | 待确认 |
| 系统测试负责人 | 待填写 | 待填写 | 待确认 |

只有第 4～5 节全部确认并完成版本冻结后，才能把草案转换为实现常量和正式 `DmaSpectrumSource` 验收基线。
